"""**일부러 틀린 것** — 라이브러리에서 시간을 읽는다 (`LNT-BAN-001`).

여기가 안 막히면 **금지가 통째로 우회된다.** 스크립트에서 `import time` 을 못 하니
라이브러리에 옮겨두고 부르면 그만이기 때문이다. 그래서 금지 패턴은 스크립트와
라이브러리에 **똑같이** 걸린다 (`schema.md` 6.5절).
"""

import time


def stamp():
    return int(time.time())
