{
  "info": {
    "description": "한 항목이 실패해도 다른 항목은 전부 돈다 — 네 상태를 한 리포트에서 본다",
    "version": "0.1.0"
  },

  "tool": {},

  "plan": [
    {
      "source": "${env.STRICTLER_EXAMPLE_ROOT}/pipelines/page_check.json",
      "description": "정상 판 — 전부 통과해야 한다",
      "config": {
        "pagePath": "${env.STRICTLER_EXAMPLE_ROOT}/targets/home.html",
        "auditLog": "${env.STRICTLER_EXAMPLE_OUT}/audit.log",
        "expectedButtonCount": 3,
        "expectedButtonLabels": ["시작하기", "문서 보기", "문의하기"],
        "expectedMenuCount": 4,
        "expectedMenuLabels": ["홈", "제품", "가격", "지원"]
      }
    },
    {
      "source": "${env.STRICTLER_EXAMPLE_ROOT}/pipelines/page_check.json",
      "description": "어긋난 판 — 위반이 두 건 나와야 한다",
      "config": {
        "pagePath": "${env.STRICTLER_EXAMPLE_ROOT}/targets/home_broken.html",
        "auditLog": "${env.STRICTLER_EXAMPLE_OUT}/audit.log",
        "expectedButtonCount": 3,
        "expectedButtonLabels": ["시작하기", "문서 보기", "문의하기"],
        "expectedMenuCount": 4,
        "expectedMenuLabels": ["홈", "제품", "가격", "지원"]
      }
    },
    {
      "source": "${env.STRICTLER_EXAMPLE_ROOT}/pipelines/page_check.json",
      "description": "없는 대상 — 오류 1건과 그 여파로 not run",
      "config": {
        "pagePath": "${env.STRICTLER_EXAMPLE_ROOT}/targets/does_not_exist.html",
        "auditLog": "${env.STRICTLER_EXAMPLE_OUT}/audit.log",
        "expectedButtonCount": 3,
        "expectedButtonLabels": ["시작하기", "문서 보기", "문의하기"],
        "expectedMenuCount": 4,
        "expectedMenuLabels": ["홈", "제품", "가격", "지원"]
      }
    },
    {
      "source": "${env.STRICTLER_EXAMPLE_ROOT}/pipelines/buttons_same.json",
      "description": "비교 파이프라인도 같은 Spec 안에서 함께 돈다",
      "report": "${env.STRICTLER_EXAMPLE_OUT}/all_in_one_compare.json",
      "config": {
        "targets": {
          "alpha": {
            "pagePath": "${env.STRICTLER_EXAMPLE_ROOT}/targets/variant_alpha.html",
            "buttonScript": "${env.STRICTLER_EXAMPLE_ROOT}/scripts/cmp_buttons_semantic.py"
          },
          "beta": {
            "pagePath": "${env.STRICTLER_EXAMPLE_ROOT}/targets/variant_beta.html",
            "buttonScript": "${env.STRICTLER_EXAMPLE_ROOT}/scripts/cmp_buttons_class.py"
          },
          "gamma": {
            "pagePath": "${env.STRICTLER_EXAMPLE_ROOT}/targets/variant_gamma_bad.html",
            "buttonScript": "${env.STRICTLER_EXAMPLE_ROOT}/scripts/cmp_buttons_role.py"
          }
        }
      }
    }
  ]
}
