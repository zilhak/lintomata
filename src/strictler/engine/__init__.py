"""파이프라인 구동 엔진.

**★ 이건 lint 다. 실행 파이프라인이 아니다.**
"실패했을 때 무엇을 해야 하나"라는 사고를 하지 않는다 — 복구·재시도·되돌아가기·
대체 경로·에러 핸들링은 전부 실행 엔진의 사고방식이고 여기 해당 없다.
실패하면 그 지점에서 진행하지 않는다. 그게 전부다.

**실패는 최대한 수집하고, 여파는 not run 이다.** 한 항목이 실패해도 다른 항목은
전부 돈다. 단 실패 지점의 뒷단을 억지로 이어가면 실패가 전파되어 원인이 뭉개지므로,
도달 불가가 된 노드를 **전수 검사해서** 싹 다 `not_run` 으로 바꾼다.

- `state` — 상태머신. **전이는 런타임이 수행하고 노드는 읽기만 한다**
- `exec` — 스크립트 하나를 로드·실행하고 input/output 을 검증
- `runtime` — 값 검증 파이프라인 구동, not run 전파, Spec 단위 실행
- `compare` — 비교 파이프라인 구동 (target 별 실행·취합·동등 비교)

⚠ stub. Step 3 에서 구현한다.
"""

from __future__ import annotations

__all__: list[str] = []
