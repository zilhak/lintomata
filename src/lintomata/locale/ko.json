{
  "Checks that a program behaves the way it was planned — QA turned into a program, lint aimed at humans": "기획대로 돌아가는지를 검사하는 도구 — QA 의 프로그램화, lint 의 인간 버전",
  "Examples:\n  lintomata script  add    ./detect_buttons.py\n  lintomata library add    ./buttons.py\n  lintomata node    update nd_e5f6a7b8 ./detect_buttons.json\n  lintomata node    test   nd_e5f6a7b8\n  lintomata pipeline list\n  lintomata check   sp_3a4b5c6d\n": "예:\n  lintomata script  add    ./detect_buttons.py\n  lintomata library add    ./buttons.py\n  lintomata node    update nd_e5f6a7b8 ./detect_buttons.json\n  lintomata node    test   nd_e5f6a7b8\n  lintomata pipeline list\n  lintomata check   sp_3a4b5c6d\n",
  "Registry path (default: $LINTOMATA_HOME, else ~/.lintomata)": "등록소 경로 (기본: $LINTOMATA_HOME 또는 ~/.lintomata)",
  "Output language. Overrides the registry `config.json` `locale`; the environment is never consulted": "출력 언어. 등록소 `config.json` 의 `locale` 보다 우선하며, 환경변수는 보지 않는다",
  "<kind|command>": "<종류|명령>",
  "script (.py) — the code that actually runs": "스크립트 (.py) — 실제 동작 코드",
  "library (.py) — functions shared by several scripts": "라이브러리 (.py) — 여러 스크립트가 나눠 쓰는 함수",
  "node (JSON) — what one step does": "노드 (JSON) — 동작 정의",
  "pipeline (JSON) — the DAG the nodes are wired into": "파이프라인 (JSON) — 노드들의 DAG 구성",
  "Spec (JSON) — the plan": "Spec (JSON) — 기획",
  "Register after the static checks. Only what passes is stored": "정적 검사 후 등록. 통과해야 저장된다",
  "Path of the file to register": "등록할 파일 경로",
  "Registered name (defaults to the file name)": "등록 이름 (생략 시 파일명)",
  "List entries. Broken ones are marked": "목록. 깨진 구성 표시",
  "Print as JSON": "JSON 으로 출력",
  "Detail — content, hash, references": "상세 — 내용·해시·참조 관계",
  "Replace the content. Keeps the id. Revalidates dependents transitively": "내용 교체. id 유지. 상위 전이적 재검증",
  "Path of the file holding the new content": "새 내용 파일 경로",
  "Delete. Not blocked even when something still references it": "삭제. 참조가 있어도 막지 않는다",
  "Node unit test (actually runs the script)": "노드 단위테스트 (실제 실행)",
  "Run the checks": "검사 실행",
  "spec id (sp_...) or a path to a Spec file": "spec id (sp_...) 또는 Spec 파일 경로",
  "Print the report as JSON": "리포트를 JSON 으로 출력",
  "Not an absolute path even after expansion: {path}": "전개 후에도 절대경로가 아닙니다: {path}",
  "Every path must be absolute. Use `~` or `${env.X}`. A path that depends on the cwd cannot be used — it is what makes the same check behave differently depending on where you ran it from": "모든 경로는 절대경로여야 합니다. `~` 또는 `${env.X}` 를 쓰세요. cwd 에 의존하는 경로는 쓸 수 없습니다",
  "The referenced environment variable is not defined: {name}": "참조한 환경변수가 정의돼 있지 않습니다: {name}",
  "Define the environment variable that `${env.X}` names in the environment you run in. Paths were pushed out into environment variables precisely so that each machine and each CI can hold a different value": "`${env.X}` 가 가리키는 환경변수를 실행 환경에 정의하세요. 머신·CI 마다 값이 달라도 되도록 경로를 환경변수로 뺀 것입니다",
  "The value of environment variable `{name}` is itself a relative path: {value}": "환경변수 `{name}` 의 값 자체가 상대경로입니다: {value}",
  "The value must be an absolute path. Something like `PROJECT_ROOT=./foo` brings the cwd dependency straight back": "환경변수 값이 절대경로여야 합니다. `PROJECT_ROOT=./foo` 같은 값은 cwd 의존을 되살립니다",
  "The value of config `{name}`, declared with `path: true`, breaks the path rule: {value}": "`path: true` 로 선언된 config `{name}` 의 값이 경로 규칙을 어깁니다: {value}",
  "This config is declared `path: true`, so the path rule applies to it. Fill it with an absolute path": "이 config 는 `path: true` 로 선언돼 경로 규칙이 적용됩니다. 절대경로를 넣으세요",
  "Cannot find the file the node points at: {script}": "노드가 가리키는 파일을 찾을 수 없습니다: {script}",
  "The node's `script` and `libraries` values must be either something registered (`${ref.sc_...}` / `${ref.lb_...}`) or a path to a file that exists": "노드의 `script` 와 `libraries` 값은 등록된 것(`${ref.sc_...}` / `${ref.lb_...}`) 또는 실재하는 파일 경로여야 합니다",
  "Cannot find the node `source` points at: {source}": "`source` 가 가리키는 노드를 찾을 수 없습니다: {source}",
  "It must be a registered node (`${ref.nd_...}`) or a node file that exists. This covers every position that names a node — a pipeline's `source`, a node unit test's `node`, and so on": "`source` 는 등록된 노드(`${ref.nd_...}`) 또는 실재하는 노드 파일이어야 합니다 (파이프라인의 `source`, 노드 단위테스트의 `node` 등 노드를 가리키는 모든 자리)",
  "`inputs` names a node id that is not in this pipeline: {name}": "`inputs` 가 이 파이프라인에 없는 노드 id 를 가리킵니다: {name}",
  "Every value in `inputs` must be the `id` of a node in the same pipeline. It is the `id`, not a path to a node file": "`inputs` 의 값은 같은 파이프라인 안의 노드 `id` 여야 합니다. 노드 파일 경로가 아니라 `id` 입니다",
  "`transitions.after` names a node id that is not in this pipeline: {name}": "`transitions.after` 가 이 파이프라인에 없는 노드 id 입니다: {name}",
  "`transitions.after` must be the `id` of a node in the same pipeline": "`transitions.after` 는 같은 파이프라인 안의 노드 `id` 여야 합니다",
  "`compare` names a node id that is not in this pipeline: {name}": "`compare` 가 이 파이프라인에 없는 노드 id 를 가리킵니다: {name}",
  "Only the `id` of a node in this pipeline can be listed in `compare`": "`compare` 에는 이 파이프라인의 노드 `id` 만 적을 수 있습니다",
  "Broken reference syntax — the namespace is missing, the namespace is unknown, or the name is empty: {ref}": "참조 문법이 깨졌습니다 — 네임스페이스가 없거나, 모르는 네임스페이스거나, 이름이 비었습니다: {ref}",
  "A reference always carries a namespace — there are exactly four: `${env.X}` / `${config.X}` / `${state.X}` / `${ref.<id>}`. Without one, \"an undefined environment variable\" and \"a misspelled config\" cannot be told apart and the error collapses into a vague one. The offending reference: {ref}": "참조는 네임스페이스를 반드시 붙입니다 — `${env.X}` / `${config.X}` / `${state.X}` / `${ref.<id>}` 넷뿐입니다. 네임스페이스가 없으면 \"미정의 환경변수인지 config 오타인지\" 구분할 수 없어 에러가 뭉개집니다. 문제의 참조: {ref}",
  "The reference syntax is fine, but it was never expanded before reaching here: {ref}": "참조 문법은 정상인데 이 자리에 도달하기 전에 전개되지 않았습니다: {ref}",
  "By this point every reference must already be resolved. Letting an unexpanded reference through as a literal turns the cause into a misleading \"file not found\" further downstream. Check that no value is missing from the `config` declaration and that the order of expansion is right. The offending reference: {ref}": "이 자리에서는 모든 참조가 이미 풀려 있어야 합니다. 전개되지 않은 참조를 리터럴로 통과시키면 나중에 \"파일 없음\" 으로 원인이 뭉개집니다. `config` 선언에 빠진 값이 없는지, 전개 순서가 맞는지 확인하세요. 문제의 참조: {ref}",
  "The DAG has a cycle.": "DAG 에 순환이 있습니다.",
  "`inputs` is what creates the dependencies. With a cycle there is no execution order to pick. The cycle: {cycle}": "`inputs` 가 의존 관계를 만듭니다. 순환이 생기면 실행 순서가 정해지지 않습니다. 순환 경로: {cycle}",
  "Node `{name}` references no node and nothing references it.": "노드 `{name}` 은 어떤 노드도 참조하지 않고 자기도 아무것도 내놓지 않습니다.",
  "This node is isolated in the graph. Wire it up with `inputs`, or remove it if it is not needed": "이 노드는 그래프에서 고립돼 있습니다. `inputs` 로 연결하거나, 필요 없으면 제거하세요",
  "`inputs` names more than one distinct upstream node: {nodes}": "`inputs` 가 서로 다른 앞단 노드를 둘 이상 가리킵니다: {nodes}",
  "`Args.input` is one field, so it takes one value. With two different upstream nodes in `inputs` there is no way to decide which one goes in. The upstream nodes: {nodes} — reduce them to one, or put a node in between that merges the two": "`Args.input` 은 필드 하나라 값도 하나만 받습니다. `inputs` 에 서로 다른 노드를 둘 이상 적으면 어느 것을 넣어야 할지 정할 수 없습니다. 문제의 앞단: {nodes} — 앞단을 하나로 줄이거나, 둘을 합치는 노드를 사이에 두세요",
  "`dict` is used as a type. (file: {file})": "`dict` 를 타입으로 썼습니다. (파일: {file})",
  "Declare every composite type as a `dataclass`. Allowing `dict` makes the dataclass requirement meaningless in a single line": "복합 타입은 반드시 `dataclass` 로 선언하세요. `dict` 를 허용하면 타입 계약이 무의미해집니다",
  "`Optional` / `None` is used as a type. (file: {file})": "`Optional` / `None` 을 타입으로 썼습니다. (파일: {file})",
  "`Optional` cannot be used. Do not declare a field that may be absent — `Args` declares only the fields it actually uses": "`Optional` 은 쓸 수 없습니다. 값이 없을 수 있는 필드는 선언하지 마세요 — `Args` 는 쓰는 필드만 선언합니다",
  "A type that is neither a primitive nor a dataclass is used: {type} (file: {file})": "primitive 도 dataclass 도 아닌 타입을 썼습니다: {type} (파일: {file})",
  "The only usable types are `int` `float` `str` `bool` `bytes` `list[T]` and a `dataclass`": "쓸 수 있는 타입은 `int` `float` `str` `bool` `bytes` `list[T]` 와 `dataclass` 뿐입니다",
  "The upstream output definition and the downstream input definition differ.": "앞단 output 정의와 뒷단 input 정의가 다릅니다.",
  "The two wired nodes declare different type definitions. The graph check demands **strict identity**. Upstream: {out} / downstream: {in}": "배선된 두 노드의 타입 정의가 다릅니다. 그래프 검사는 **엄격한 동일성**을 요구합니다. 앞단: {out} / 뒷단: {in}",
  "The `type` of a `config` is not in the allowed set: {type}": "`config` 의 `type` 이 허용 집합에 없습니다: {type}",
  "A `config` `type` uses the same vocabulary a script does — `str` `int` `float` `bool` `bytes` `list[T]`": "`config` 의 `type` 은 스크립트와 같은 어휘를 씁니다 — `str` `int` `float` `bool` `bytes` `list[T]`",
  "Taking the union of a subset connected component, one field name carries two different types: {field}": "부분집합 연결 성분을 합집합 낼 때 같은 필드명의 타입이 갈립니다: {field}",
  "Field `{field}` is typed differently across the merge candidates {names} ({types}). Dataclasses in a subset relation are merged into one larger dataclass, so the same field name must carry the same type. If the concepts differ, give the fields different names": "병합 대상 {names} 에서 필드 `{field}` 의 타입이 갈립니다 ({types}). 부분집합 관계인 dataclass 들은 하나의 큰 dataclass 로 합쳐지므로, 같은 필드명은 같은 타입이어야 합니다. 개념이 다르면 필드명을 다르게 하세요",
  "A dataclass references itself, directly or indirectly.": "dataclass 가 자기 자신을 (직접·간접으로) 참조합니다.",
  "`{cycle}` is the cycle. Types normalise their nesting from the bottom up, so a recursive type cannot be declared. If you need a tree, flatten it into a `list[T]` whose items carry a parent id field": "`{cycle}` 이 순환 참조입니다. 타입은 중첩을 바닥부터 정규화하므로 재귀 타입을 선언할 수 없습니다. 트리 구조가 필요하면 `list[T]` 를 평평하게 펴서 부모 id 를 필드로 갖는 형태로 바꾸세요",
  "No `Args` dataclass is declared. (file: {file})": "`Args` dataclass 가 선언돼 있지 않습니다. (파일: {file})",
  "Every node script defines a dataclass named `Args` and takes the form `runNode(args: Args)`": "모든 노드 스크립트는 `Args` 라는 이름의 dataclass 를 정의하고 `runNode(args: Args)` 형태여야 합니다",
  "The entry point `runNode` is missing or has the wrong shape. (file: {file})": "진입점 `runNode` 가 없거나 형태가 다릅니다. (파일: {file})",
  "The entry point name is fixed as `runNode`. It takes exactly one argument and that argument's type must be `Args`": "진입점 이름은 `runNode` 로 고정입니다. 인자는 하나이고 타입은 `Args` 여야 합니다",
  "No dataclass leaves through `returnResult()` — either the call is missing, or the output type is not a dataclass (a primitive, or undetermined). (file: {file})": "`returnResult()` 로 dataclass 를 내보내지 않습니다 — 호출이 없거나 출력 타입이 dataclass 가 아닙니다(primitive·미확정). (파일: {file})",
  "Output leaves through `returnResult()`. The return type is a dataclass and its name is free — type identity is decided **by structure**, so a primitive cannot be sent out as is": "출력은 `returnResult()` 로 내보냅니다. 반환 타입은 dataclass 이고 이름은 자유입니다 — 타입 동일성을 **구조로** 판정하므로 primitive 를 그대로 내보낼 수 없습니다",
  "`Args` has a field other than `input` / `params` / `state`: {names}": "`Args` 에 `input`/`params`/`state` 외의 필드가 있습니다: {names}",
  "`Args` may hold only those three fields. Declare only the ones you use": "`Args` 는 `input` / `params` / `state` 세 필드만 가질 수 있습니다. 쓰는 것만 선언하세요",
  "This is a Reckon node, but `Args.params` has no expected-value field. (file: {file})": "Reckon 노드인데 `Args.params` 에 기댓값 필드가 없습니다. (파일: {file})",
  "A Reckon takes its expected value from the Spec. Hard-coding it in the script turns the Spec file into an empty shell — editing the plan would no longer change the decision. Declare the expected-value field on `Args.params`": "Reckon 은 기댓값을 Spec 에서 받아야 합니다. 스크립트에 하드코딩하면 기획 파일이 껍데기가 됩니다. `Args.params` 에 기댓값 필드를 선언하세요",
  "This is an Action node, but its input type and output type differ. (file: {file})": "Action 노드인데 input 타입과 output 타입이 다릅니다. (파일: {file})",
  "An Action passes data straight through. The `Args.input` type and the return type must be the same. If you need to transform, use a Perceive": "Action 은 데이터를 그대로 통과시킵니다. `Args.input` 타입과 반환 타입이 같아야 합니다. 변환이 필요하면 Perceive 를 쓰세요",
  "This is a Reckon node, but its output dataclass has no decision field `passed: bool`. (file: {file})": "Reckon 노드인데 출력 dataclass 에 판정 필드 `passed: bool` 이 없습니다. (파일: {file})",
  "A Reckon is the node that **decides** — the engine needs `passed: bool` on the output dataclass to tell `pass` from `violation`. Without it nobody finds out until the run, and at that point you get an error instead of a report": "Reckon 은 **판정**을 내는 노드입니다 — 출력 dataclass 에 `passed: bool` 필드가 있어야 엔진이 통과/위반을 가릅니다. 이게 없으면 실행할 때까지 아무도 모르고, 그때는 리포트가 아니라 오류가 납니다",
  "A user state name uses the `__` prefix: {name}": "사용자 상태 이름에 `__` 접두를 썼습니다: {name}",
  "The `__` prefix is reserved for engine-provided fields (`__startedAt` and the like). Pick another name": "`__` 접두는 엔진 제공 필드 전용입니다 (`__startedAt` 등). 다른 이름을 쓰세요",
  "A state the node requires is not bound in `states`.": "노드가 요구하는 상태가 `states` 에 매핑되지 않았습니다.",
  "Every field on the node's `Args.state` must be bound to a pipeline state name. Missing: {names}": "노드의 `Args.state` 필드마다 파이프라인 상태 이름을 매핑해야 합니다. 누락: {names}",
  "The bound target is not in `states.values`: {name}": "매핑 대상이 `states.values` 에 없습니다: {name}",
  "The name you bound to is not in the pipeline's state set. Add it to `states.values`, or fix the name": "매핑한 이름이 파이프라인 상태 집합에 없습니다. `states.values` 에 추가하거나 이름을 고치세요",
  "`when` references a state the script never declared: {name}": "`when` 이 스크립트가 선언하지 않은 상태를 참조합니다: {name}",
  "`when` is written in the node's own vocabulary. That name must be declared on the script's `Args.state`": "`when` 은 노드 자기 어휘로 씁니다. 그 이름이 스크립트의 `Args.state` 에 선언돼 있어야 합니다",
  "`transitions.to` names a state that is not in `states.values`: {name}": "`transitions.to` 가 `states.values` 에 없는 상태입니다: {name}",
  "`transitions.to` must be a state listed in `states.values`": "`transitions.to` 는 `states.values` 에 있는 상태여야 합니다",
  "No transition leads to the state `when` references.": "`when` 이 참조하는 상태로 가는 transition 이 없습니다.",
  "The node vocabulary `{name}` is bound to pipeline state `{mapped}`, and no `transitions` entry leads to that state, so the node never runs. Add the transition or drop the `when` — a transition is written in the pipeline vocabulary, on the `{mapped}` side": "노드 어휘 `{name}` 은 파이프라인 상태 `{mapped}` 에 매핑돼 있는데, 그 상태로 가는 `transitions` 가 없어 노드가 영원히 실행되지 않습니다. 전이를 추가하거나 `when` 을 지우세요 (전이를 적는 자리는 파이프라인 어휘 `{mapped}` 쪽입니다)",
  "Running the state machine shows node `{name}` is unreachable.": "상태머신을 돌려보니 노드 `{name}` 에 도달할 수 없습니다.",
  "Running the conditions together with the graph, nothing ever reaches this node. Check whether its `when` state is true only before this node's inputs have finished": "조건과 그래프를 함께 돌려본 결과 이 노드에 도달할 수 없습니다. `when` 상태가 이 노드의 입력이 끝나기 전에만 참인지 확인하세요",
  "A function whose result changes with time is used: {name} (file: {file})": "시간에 따라 결과가 달라지는 함수를 썼습니다: {name} (파일: {file})",
  "A script cannot read the clock. If you need the run time, use `Args.state.__startedAt` (epoch ms)": "스크립트 안에서 시간을 읽을 수 없습니다. 실행 시각이 필요하면 `Args.state.__startedAt` (epoch ms) 을 쓰세요",
  "Randomness is used: {name} (file: {file})": "랜덤을 썼습니다: {name} (파일: {file})",
  "Randomness is banned in every node type. The same input has to produce the same result, or the report cannot be trusted": "랜덤은 전 노드 금지입니다. 같은 입력에 같은 결과가 나와야 리포트를 믿을 수 있습니다",
  "`subprocess` / `exec` and the like are called directly: {name} (file: {file})": "`subprocess` / `exec` 류를 직접 호출했습니다: {name} (파일: {file})",
  "Running arbitrary commands is banned. If you need an external tool, declare its path and allowed functions under the Spec's `tool` and go through that": "임의 명령 실행은 금지입니다. 외부 도구가 필요하면 Spec 의 `tool` 에 경로와 허용 함수를 선언하고 그것을 쓰세요",
  "A state that is not on `Args.state` is referenced: {name} (file: {file})": "`Args.state` 에 없는 상태를 참조했습니다: {name} (파일: {file})",
  "Declare the state on `Args.state` before referencing it. What is not declared cannot be used": "참조할 상태를 `Args.state` 에 미리 선언해야 합니다. 선언에 없는 것은 쓸 수 없습니다",
  "A package declared in the PEP 723 header is not present in this environment: {requirement} (file: {file})": "PEP 723 헤더에 선언한 패키지가 이 환경에 없습니다: {requirement} (파일: {file})",
  "A node script is loaded into the **same process** as lintomata, so its `import` resolves in the environment lintomata itself is installed in. No isolated environment is created for you — install it there as well: {install}\n⚠ `--with` is **declarative**: only what you list survives. The command above already carries everything the registry declares, so use it **verbatim**. Listing a subset wipes out the rest": "노드 스크립트는 lintomata 와 **같은 프로세스**에 로드되므로 `import` 가 lintomata 가 설치된 환경에서 풀립니다. 격리 환경을 만들어 주지 않으니 그 환경에 함께 설치하세요: {install}\n⚠ `--with` 는 **선언적**이라 적은 것만 남습니다 — 위 명령은 등록소에 선언된 것을 전부 포함하고 있으니 **그대로** 쓰세요. 일부만 적으면 나머지가 지워집니다",
  "Cannot read the PEP 723 header: {reason} (file: {file})": "PEP 723 헤더를 읽을 수 없습니다: {reason} (파일: {file})",
  "The header opens with `# /// script` and closes with `# ///`, and every line between them is TOML behind a `# `. `dependencies` is an array of PEP 508 strings:\n  # /// script\n  # requires-python = \">=3.11\"\n  # dependencies = [\"selectolax>=0.3\"]\n  # ///\nHaving no header at all is fine — a script that only uses the stdlib does not need one": "헤더는 `# /// script` 로 열고 `# ///` 로 닫으며, 사이의 각 줄은 `# ` 로 시작하는 TOML 입니다. `dependencies` 는 PEP 508 문자열의 배열입니다:\n  # /// script\n  # requires-python = \">=3.11\"\n  # dependencies = [\"selectolax>=0.3\"]\n  # ///\n헤더가 아예 없어도 됩니다 — stdlib 만 쓰는 스크립트에는 필요 없습니다",
  "The installed version does not satisfy the declared requirement: {requirement} (installed: {installed}) (file: {file})": "설치된 버전이 선언한 요구를 만족하지 않습니다: {requirement} (설치된 것: {installed}) (파일: {file})",
  "Only one copy of a package is installed in the environment. Either bring the installed one up to the requirement ({install}), or bring the header's requirement in line with the version you actually use.\n⚠ `--with` is **declarative**: only what you list survives. The command above already carries everything the registry declares, so use it **verbatim**. Listing a subset wipes out the rest": "환경에는 패키지가 한 벌만 깔립니다. 설치된 것을 요구에 맞추거나 ({install}) 헤더의 요구를 실제로 쓰는 버전에 맞추세요.\n⚠ `--with` 는 **선언적**이라 적은 것만 남습니다 — 위 명령은 등록소에 선언된 것을 전부 포함하고 있으니 **그대로** 쓰세요. 일부만 적으면 나머지가 지워집니다",
  "A function that is not in the Spec's `tool` is called: {name}": "Spec 의 `tool` 에 없는 함수를 호출했습니다: {name}",
  "Every external tool call must have its function name declared under the Spec's `tool`": "외부 도구 호출은 Spec 의 `tool` 에 함수명을 선언해야 합니다",
  "The executable path passed as an argument is not declared under `tool`.": "인자로 준 실행파일 경로가 `tool` 에 선언돼 있지 않습니다.",
  "The executable path handed to the function must match the `path` under `tool`. Given: {path}": "함수에 넘긴 실행파일 경로가 `tool` 의 `path` 와 일치해야 합니다. 준 값: {path}",
  "The Spec does not fill a config declared `required: true`.": "`required: true` 인 config 를 Spec 이 채우지 않았습니다.",
  "Fill the config the pipeline requires in the Spec's `plan` entry. Missing: {names}": "파이프라인이 요구하는 config 를 Spec 의 `plan` 항목에서 채우세요. 누락: {names}",
  "The value filled into config `{name}` has a different type than declared.": "config `{name}` 에 채운 값의 타입이 선언과 다릅니다.",
  "Declared type: {declared} / given value: {given}": "선언된 타입: {declared} / 준 값: {given}",
  "A config the pipeline never declared is filled: {name}": "파이프라인이 선언하지 않은 config 를 채웠습니다: {name}",
  "Only a config the pipeline declares can be filled. Either this is a typo, or the declaration is missing on the pipeline side": "파이프라인이 선언한 config 만 채울 수 있습니다. 오타이거나 파이프라인 쪽 선언이 빠진 것입니다",
  "The pipeline is `kind: compare`, but the Spec entry has no `report`.": "`kind: compare` 인데 Spec 항목에 `report` 가 없습니다.",
  "A compare pipeline builds its result as it runs, so it needs somewhere to put it. Point `report` at that place in the `plan` entry": "비교 파이프라인은 결과를 실행과 동시에 쌓으므로 출력 위치가 필요합니다. `plan` 항목에 `report` 를 지정하세요",
  "The per-target scripts declare different input/output/state types: {node}": "target 별 스크립트가 서로 다른 input/output/state 타입을 선언했습니다: {node}",
  "The recognition script may differ per target, but the **input/output/state types belong to the node and must be shared** or there is nothing to compare. `params` may differ": "인식 스크립트는 target 마다 달라도 되지만, **input/output/state 타입은 노드에 귀속되어 공통**이어야 비교가 성립합니다. `params` 는 달라도 됩니다",
  "`targets` holds fewer than two entries: {count}": "`targets` 가 2개 미만입니다: {count}",
  "Comparing takes at least two targets. There is no upper bound": "비교하려면 대상이 둘 이상이어야 합니다. 개수 상한은 없습니다",
  "A config the target requires is in neither `targets.<target>` nor the shared config.": "target 이 요구하는 config 가 `targets.<target>` 에도 공통에도 없습니다.",
  "`${config.X}` is looked up in `targets.<the current target>` first, and in the shared config only if it is not there. It is in neither: {name}": "`${config.X}` 는 `targets.<현재target>` 에서 먼저 찾고 없으면 공통에서 찾습니다. 둘 다 없습니다: {name}",
  "The `args` fixture does not match the script's `Args` declaration.": "`args` fixture 가 스크립트의 `Args` 선언에 맞지 않습니다.",
  "**The test definition is what is wrong here**, not the script. Bring the fixture in line with the `Args` declaration": "**테스트 정의가 잘못됐습니다** (스크립트가 아니라). fixture 를 `Args` 선언에 맞추세요",
  "`runNode` raised.": "`runNode` 가 예외를 냈습니다.",
  "The script ended in an exception: {exc}": "스크립트가 예외로 끝났습니다: {exc}",
  "The returned value does not match the declared output type.": "반환값이 선언된 출력 타입에 맞지 않습니다.",
  "The declared output type and what actually came back differ. Declared: {declared} / actual: {actual}": "선언한 출력 타입과 실제 반환값이 다릅니다. 선언: {declared} / 실제: {actual}",
  "`expect` and the actual value differ.": "`expect` 와 실제 값이 다릅니다.",
  "Expected: {expect} / actual: {actual}": "기대: {expect} / 실제: {actual}",
  "This is an Action node, but the returned value differs from the input.": "Action 노드인데 반환값이 입력과 다릅니다.",
  "An Action must pass data straight through. Cause the side effect and leave the value alone": "Action 은 데이터를 그대로 통과시켜야 합니다. 부작용만 일으키고 값은 건드리지 마세요",
  "The Reckon test has no pass/violation contrast pair.": "Reckon 테스트에 통과/위반 대조쌍이 없습니다.",
  "Put in one passing case and one violating case that share the same `input` and differ only in `params` — that pair is what proves the expected value is actually used": "`input` 이 같고 `params` 만 다른 통과 케이스와 위반 케이스를 각각 두면 기댓값이 실제로 쓰이는지 검증됩니다",
  "Both halves of the contrast pair decide the same way.": "대조쌍의 판정이 같습니다.",
  "The expected value changed and the decision did not — the script is ignoring it and deciding on something hard-coded": "기댓값을 바꿨는데 판정이 안 바뀝니다 — 기댓값을 쓰지 않고 하드코딩하고 있습니다",
  "The unit test's `node` points at **a different node than the one requested**.": "단위테스트의 `node` 가 **요청한 노드와 다른 노드**를 가리킵니다.",
  "When called as `lintomata node test <id>`, **the node with that id is authoritative**. If the test definition's `node` points somewhere else, a node you never asked for gets run and the report is **false**. Requested: {requested} / what the test points at: {declared}": "`lintomata node test <id>` 로 부르면 **그 id 의 노드가 정본**입니다. 테스트 정의의 `node` 가 다른 것을 가리키면 요청하지 않은 노드를 돌려 **거짓 리포트**가 됩니다. 요청: {requested} / 테스트가 가리키는 것: {declared}",
  "The registry file's hash differs from the one taken at registration: {id}": "등록소 파일의 해시가 등록 당시와 다릅니다: {id}",
  "The registered file was changed without going through the checks. Remove it and register it again": "등록된 파일이 검사를 거치지 않고 변경됐습니다. 삭제 후 재등록하세요",
  "The reference is not in the registry: {id}": "참조가 등록소에 없습니다: {id}",
  "No such id — it was removed, or it is a typo. Check with `lintomata list`: {id}": "참조한 id 가 없습니다 (삭제됐거나 오타). `lintomata list` 로 확인하세요: {id}",
  "The reference prefix is not the kind this position requires.": "참조의 접두가 그 자리가 요구하는 종류와 다릅니다.",
  "This position takes {expected}. Given: {given} (prefixes: `sc_`=script `lb_`=library `nd_`=node `pl_`=pipeline `sp_`=Spec)": "이 자리에는 {expected} 가 와야 합니다. 준 것: {given} (접두 `sc_`=스크립트 `lb_`=라이브러리 `nd_`=노드 `pl_`=파이프라인 `sp_`=Spec)",
  "Broken reference — what it points at was deleted: {id}": "참조 대상이 삭제되어 구성이 깨졌습니다: {id}",
  "The target of the reference was deleted, so this configuration no longer holds. The missing reference: {id}. Register the target again and point this entry at the new id": "참조 대상이 삭제되어 구성이 깨졌습니다. 없어진 참조: {id}. 대상을 다시 등록하고 이 요소의 참조를 새 id 로 고치세요",
  "Broken validation — what it points at was modified and this entry no longer passes: {id}": "참조 대상이 수정되어 상위 검증이 더는 통과하지 않습니다: {id}",
  "The target of the reference was modified, which invalidated this configuration's validation. The rule that failed: {rule}. Fix this entry and `update` it again": "참조 대상이 수정되어 이 구성의 검증이 무효화됐습니다. 실패한 규칙: {rule}. 이 요소를 고쳐 다시 `update` 하세요",
  "A library slot the script requires is not wired on the node: {names}": "스크립트가 요구하는 라이브러리 슬롯이 노드에 배선되지 않았습니다: {names}",
  "`from lintomata_lib import <name>` in the script is a **capability declaration** (\"I need this slot\"); what goes into the slot is decided by the **node**. Put `\"libraries\": { \"<name>\": \"${ref.lb_...}\" }` in the node JSON — an absolute path (`${env.X}` included) works too. The unwired slots: {names}": "스크립트의 `from lintomata_lib import <이름>` 은 **능력 선언**(\"이 슬롯이 필요합니다\")이고, 그 슬롯에 무엇을 쓸지는 **노드가** 정합니다. 노드 JSON 에 `\"libraries\": { \"<이름>\": \"${ref.lb_...}\" }` 를 넣으세요 — 절대경로(`${env.X}` 포함)도 됩니다. 배선이 빠진 슬롯: {names}",
  "The script does not use a library slot the node wired: {names}": "노드가 배선한 라이브러리 슬롯을 스크립트가 쓰지 않습니다: {names}",
  "A node's `libraries` answers only the slots the script asked for with `from lintomata_lib import <name>`. An unused wiring only widens the reference graph, so **editing that library revalidates nodes that have nothing to do with it**. Drop the wiring, or actually use it in the script. The leftover wiring: {names}": "노드의 `libraries` 는 스크립트가 `from lintomata_lib import <이름>` 으로 요구한 슬롯에만 답합니다. 쓰지 않는 배선은 참조 그래프만 넓혀 **라이브러리를 고칠 때 상관없는 노드까지 재검증**하게 만듭니다. 그 배선을 빼거나, 스크립트에서 실제로 쓰세요. 남는 배선: {names}",
  "A library imports another library: {name}": "라이브러리가 다른 라이브러리를 import 합니다: {name}",
  "Libraries are **one layer only** — allow libraries to import each other and from that point on you are building a package manager. Keep the function in this file, or let the script that uses them wire both slots itself": "라이브러리는 **한 층뿐**입니다 — 라이브러리끼리 import 를 허용하면 그때부터 패키지 매니저를 만들게 됩니다. 그 함수를 이 파일 안에 두거나, 쓰는 쪽 스크립트가 두 슬롯을 각각 배선하세요",
  "A library declares a `dataclass`: {name}": "라이브러리가 `dataclass` 를 선언했습니다: {name}",
  "A v1 library provides **functions only**. If a contract type between nodes is born outside a script, contract extraction — which parses a single file — leaves a hole in the type registry. **Move this dataclass into the script** that uses it": "v1 의 라이브러리는 **함수만** 제공합니다. 노드 간 계약 타입이 스크립트 밖에서 생기면 계약 추출이 파일 하나만 파싱하므로 타입 레지스트리에 구멍이 납니다. 이 dataclass 는 그것을 쓰는 **스크립트로 옮기세요**",
  "`lintomata_lib` is imported in a form that is not allowed: {form}": "`lintomata_lib` 를 허용되지 않은 형태로 import 했습니다: {form}",
  "The only allowed form is **`from lintomata_lib import <name>` at module top level**. `import lintomata_lib`, `from lintomata_lib.<x> import y`, `import *`, and an import inside a function all make the slots **impossible to extract statically**, which makes the wiring check meaningless": "허용되는 형태는 **모듈 최상단의 `from lintomata_lib import <이름>`** 하나뿐입니다. `import lintomata_lib` 나 `from lintomata_lib.<x> import y`, `import *`, 함수 안에서의 import 는 **슬롯을 정적으로 뽑을 수 없어** 배선 검사가 무의미해집니다",
  "No such file to register: {path}\nCheck the path. The registry keeps a copy, so the original must exist at registration time.": "등록할 파일이 없습니다: {path}\n경로를 확인하세요. 등록소는 파일을 복사해 보관하므로 원본이 있어야 합니다.",
  "The file to register is not UTF-8: {path} ({reason}, byte {offset})\nOnly `.py` scripts and `.json` documents can be registered, and both must be UTF-8.": "등록할 파일이 UTF-8 이 아닙니다: {path} ({reason}, byte {offset})\n등록 대상은 `.py` 스크립트와 `.json` 문서뿐이고 둘 다 UTF-8 이어야 합니다.",
  "Cannot read the JSON: {path} ({detail})\nNodes, pipelines and Specs are all JSON documents. Fix the syntax.": "JSON 을 읽을 수 없습니다: {path} ({detail})\n노드·파이프라인·Spec 은 전부 JSON 문서입니다. 문법을 확인하세요.",
  "The top level of the JSON is not an object: {path}\nAll four layers are documents whose top level is an object (`{...}`).": "JSON 의 최상위가 객체가 아닙니다: {path}\n네 층 문서는 전부 최상위가 객체(`{...}`)입니다.",
  "`libraries.{slot}` is not a string: {value}": "`libraries.{slot}` 가 문자열이 아닙니다: {value}",
  "Node": "노드",
  "Pipeline": "파이프라인",
  "pipeline": "파이프라인",
  "node": "노드",
  "node unit test": "노드 단위테스트",
  "(top level)": "(최상위)",
  "(none)": "(없음)",
  "(not declared)": "(선언 없음)",
  "(no explanatory field)": "(설명 필드 없음)",
  "none": "없음",
  "pass": "통과",
  "violation": "위반",
  "broken reference": "참조 깨짐",
  "broken validation": "검증 깨짐",
  "The {label} JSON does not match the schema — `{where}`: {detail}\nInput: {input}\nUndeclared keys are rejected (`extra=forbid`) and required keys cannot be omitted. Follow the {label} structure in `schema.md` exactly.": "{label} JSON 이 스키마에 맞지 않습니다 — `{where}`: {detail}\n입력값: {input}\n선언되지 않은 키는 쓸 수 없고(`extra=forbid`), 필수 키는 빠질 수 없습니다. `schema.md` 의 {label} 구조를 그대로 따르세요.",
  "The node's `script` is not a string: {value}\n`script` takes the absolute path of the script file or `${ref.sc_...}`.": "노드의 `script` 가 문자열이 아닙니다: {value}\n`script` 에는 스크립트 파일의 절대경로 또는 `${ref.sc_...}` 가 옵니다.",
  "The script is not UTF-8: {path} ({reason}, byte {offset})\nA node script is Python source and must be UTF-8.": "스크립트가 UTF-8 이 아닙니다: {path} ({reason}, byte {offset})\n노드 스크립트는 Python 소스이고 UTF-8 이어야 합니다.",
  "Cannot read the script: {path} ({detail})\nCheck the file permissions.": "스크립트를 읽을 수 없습니다: {path} ({detail})\n파일 권한을 확인하세요.",
  "Cannot read the script: {path} ({detail})\nA node script is Python source and must be UTF-8.": "스크립트를 읽을 수 없습니다: {path} ({detail})\n노드 스크립트는 Python 소스이고 UTF-8 이어야 합니다.",
  "`{type}` (node {node})": "`{type}` (노드 {node})",
  "`states.initial` is not in `states.values`: {initial}\nDeclared states: {values}. If the initial state is outside the state set, no `when` can ever be decided.": "`states.initial` 이 `states.values` 에 없습니다: {initial}\n선언된 상태: {values}. 초기 상태가 상태 집합 밖이면 어떤 `when` 도 판정할 수 없습니다.",
  "`kind: verify`, yet `targets` / `compare` are filled in.\nThose two belong to `kind: compare` only. If this is a compare pipeline, set `info.kind` to `compare`; otherwise delete both sections.": "`kind: verify` 인데 `targets` / `compare` 가 채워져 있습니다.\n이 둘은 `kind: compare` 전용입니다. 비교 파이프라인이면 `info.kind` 를 `compare` 로 바꾸고, 아니면 두 섹션을 지우세요.",
  "Duplicate node id: {id}\n`inputs` wires nodes by id, so an id must be unique within the pipeline. Rename one of them.": "노드 id 가 중복됩니다: {id}\n`inputs` 가 id 로 배선을 만드므로 id 는 파이프라인 안에서 유일해야 합니다. 한쪽 이름을 바꾸세요.",
  "Cannot read the node file: {path} ({detail})\nA node file must be UTF-8 JSON.": "노드 파일을 읽을 수 없습니다: {path} ({detail})\n노드 파일은 UTF-8 JSON 이어야 합니다.",
  "The top level of the node file is not an object: {path}\nIt must be a JSON object with `info` / `type` / `script`.": "노드 파일의 최상위가 객체가 아닙니다: {path}\n`info` / `type` / `script` 를 갖는 JSON 객체여야 합니다.",
  "It is run time and the node's `script` is still unexpanded: {value}\nFill this value in from the Spec's `config` (or `targets.<name>`).": "실행 시점인데 노드의 `script` 가 아직 안 풀렸습니다: {value}\nSpec 의 `config`(또는 `targets.<이름>`)에서 이 값을 채우세요.",
  "Cannot parse the script: {path} (line {line}: {detail}). A node script must be a syntactically valid Python file — the checker never runs the file, it only reads it with `ast`, so a syntax error stops the check itself.": "스크립트를 파싱할 수 없습니다: {path} ({line}행: {detail}). 노드 스크립트는 문법이 올바른 파이썬 파일이어야 합니다 — 검사기는 파일을 돌리지 않고 `ast` 로 읽기만 하므로 문법 오류는 검사 자체를 막습니다.",
  "`from {namespace} import {names}` inside a function/class": "함수/클래스 안의 `from {namespace} import {names}`",
  "`--home` is not an absolute path: {path}\nA registry path must not depend on cwd. Give an absolute path such as `/srv/lintomata` or `~/.lintomata`.": "`--home` 이 절대경로가 아닙니다: {path}\n등록소 경로는 cwd 와 무관해야 합니다. `/srv/lintomata` 나 `~/.lintomata` 처럼 절대경로로 주세요.",
  "This position takes a {expected}, but {id} is a {actual}.\nThe prefix tells the kind (`sc_`=script `lb_`=library `nd_`=node `pl_`=pipeline `sp_`=Spec). Call `lintomata {actual} <command>` instead.": "이 자리에는 {expected} 가 와야 하는데 {id} 는 {actual} 입니다.\n접두가 종류를 말합니다 (`sc_`=스크립트 `lb_`=라이브러리 `nd_`=노드 `pl_`=파이프라인 `sp_`=Spec). `lintomata {actual} <명령>` 으로 부르세요.",
  "✕ broken reference — {id} is gone": "✕ 참조 깨짐 — {id} 없음",
  "✕ broken validation — {detail}": "✕ 검증 깨짐 — {detail}",
  "Not registered — only what passes the static checks is stored: {path}": "등록하지 않았습니다 — 정적 검사를 통과해야 저장됩니다: {path}",
  "{id}  {name}  ({kind}) registered": "{id}  {name}  ({kind}) 등록됨",
  "registry  {home}": "등록소  {home}",
  "No {kind} is registered.": "등록된 {kind} 가 없습니다.",
  "  (nobody uses it)": "  (아무도 쓰지 않음)",
  "  status         {value}": "  상태           {value}",
  "  registry       {value}": "  등록소         {value}",
  "  hash           {value}": "  해시           {value}",
  "  registered at  {value}": "  등록시각       {value}",
  "  file           {value}": "  파일           {value}",
  "  unit test      {value}": "  단위테스트     {value}",
  "  declared deps  {value}": "  선언 의존성    {value}",
  "  references     {value}": "  참조하는 것    {value}",
  "  referenced by  {value}{unused}": "  참조하는 상위  {value}{unused}",
  "--- content ---": "--- 내용 ---",
  "Not updated — the new content must pass the static checks too: {path}": "수정하지 않았습니다 — 새 내용도 정적 검사를 통과해야 합니다: {path}",
  "{id}  {name}  content replaced (id kept)": "{id}  {name}  내용 교체됨 (id 유지)",
  "Revalidating the dependents turned up broken validation — the update itself went through:": "상위 재검증에서 검증 깨짐이 나왔습니다 — 수정 자체는 성사됐습니다:",
  "Deleted {id}.": "{id} 를 삭제했습니다.",
  "This deletion left broken references behind (deletion is never blocked):": "이 삭제로 참조가 깨진 구성이 있습니다 (삭제는 막지 않습니다):",
  "No node unit test is registered for this node: {id}\nThe test lives in `<node file>.test.json` and **is copied along with the node** — put it next to the node file under that name and run `lintomata node add`/`update` again, and it goes into the registry with it. To run it without registering, pass the file path directly: `lintomata node test /abs/path/detect_buttons.test.json`": "이 노드에는 단위테스트가 등록돼 있지 않습니다: {id}\n테스트 정의는 `<노드파일>.test.json` 이고 **노드와 함께 복사된다** — 노드 파일 옆에 그 이름으로 두고 `lintomata node add`/`update` 를 다시 하면 등록소에 함께 들어갑니다. 등록 없이 돌리려면 파일 경로를 직접 주세요: `lintomata node test /abs/path/detect_buttons.test.json`",
  "No such node unit test file: {path}\nPass a node id (`nd_...`) or the path of a `<node file>.test.json`.": "노드 단위테스트 파일이 없습니다: {path}\n노드 id (`nd_...`) 또는 `<노드파일>.test.json` 경로를 주세요.",
  "No such Spec file: {path}\nA registered Spec is passed by id (`sp_...`); an unregistered one by file path.": "Spec 파일이 없습니다: {path}\n등록된 Spec 은 id (`sp_...`) 로, 등록 안 한 것은 파일 경로로 줍니다.",
  "Cannot read the Spec file: {path} ({detail})\nA Spec must be UTF-8 JSON.": "Spec 파일을 읽을 수 없습니다: {path} ({detail})\nSpec 은 UTF-8 JSON 이어야 합니다.",
  "there are {count} `{type}` blocks": "`{type}` 블록이 {count}개입니다",
  "cannot read the TOML ({detail})": "TOML 을 읽을 수 없습니다 ({detail})",
  "`requires-python` is not a string": "`requires-python` 이 문자열이 아닙니다",
  "`dependencies` is not an array": "`dependencies` 가 배열이 아닙니다",
  "an element of `dependencies` is not a string": "`dependencies` 의 원소가 문자열이 아닙니다",
  "cannot read `{item}` in `dependencies` ({detail})": "`dependencies` 의 `{item}` 를 읽을 수 없습니다 ({detail})",
  "This module is declared as `{item}` in the script's PEP 723 header, but it is not in the current environment. A node script resolves its `import`s in the same environment as lintomata — install it there: {command}\n⚠ `--with` is **declarative**: only what you list survives. If other scripts need packages too, list **all of them together** (see the declared deps in `lintomata script list` / `show <id>`).": "이 모듈은 스크립트의 PEP 723 헤더에 `{item}` 로 선언돼 있지만 지금 환경에 없습니다. 노드 스크립트는 lintomata 와 같은 환경에서 `import` 가 풀립니다 — 그쪽에 함께 설치하세요: {command}\n⚠ `--with` 는 **선언적**이라 적은 것만 남습니다. 다른 스크립트가 요구하는 것이 있으면 **전부 함께** 적으세요 (`lintomata script list` / `show <id>` 의 선언 의존성).",
  "The `{root}` package is installed, but it has no `{module}` inside. This is not an installation problem — **the module path is wrong**. Check the name, or check whether that version of the package has that module.": "`{root}` 패키지는 설치돼 있으나 그 안에 `{module}` 이 없습니다. 설치 문제가 아니라 **모듈 경로가 틀린 것**입니다 — 이름을 확인하거나, 그 패키지 버전에 그 모듈이 있는지 확인하세요.",
  "The compare engine was handed a `kind: {kind}` pipeline.\nCompare driving is only for pipelines whose `info.kind` is `compare`. Run a verify pipeline on the verify engine.": "비교 엔진이 `kind: {kind}` 파이프라인을 받았습니다.\n비교 구동은 `info.kind` 가 `compare` 인 파이프라인 전용입니다. 값 검증 파이프라인은 값 검증 엔진으로 돌리세요.",
  "It is run time and the node's `script` is still unexpanded: {value} (target: {target})\nFill this value in from the Spec's `config.targets.<name>` or from the shared `config`.": "실행 시점인데 노드의 `script` 가 아직 안 풀렸습니다: {value} (target: {target})\nSpec 의 `config.targets.<이름>` 또는 공통 `config` 에서 이 값을 채우세요.",
  "This node's script is not ready on some targets: {targets}\nA comparison only holds once every target has produced a value. Fix the errors above first.": "이 노드의 스크립트가 일부 target 에서 준비되지 않았습니다: {targets}\n비교는 target 전부의 값이 모여야 성립합니다. 위의 오류를 먼저 고치세요.",
  "The script for target `{target}` declares no output type: {path}\nThe output must leave through `returnResult()` as a dataclass — the comparison reads that structure.": "target `{target}` 의 스크립트가 출력 타입을 선언하지 않았습니다: {path}\n출력은 `returnResult()` 로 dataclass 를 내보내야 합니다 — 비교는 그 구조를 보고 합니다.",
  "All {count} targets produced the same value: {targets}": "대상 {count}개가 같은 값을 내놨습니다: {targets}",
  "The targets produced different outputs.\n{outputs}\nThe decision is whether every target in the list yields the same value — one mismatch is a violation. If a difference is safe to ignore (rounded coordinates, timestamps …), normalize it in the script that emits the data for comparison. The engine only knows `==`.": "대상 간 출력이 다릅니다.\n{outputs}\n판정은 목록 전부가 같은 값을 뱉느냐입니다 — 하나만 어긋나도 위반입니다. 무시해도 되는 차이(좌표 반올림·타임스탬프 등)라면 비교용 데이터를 내보내는 스크립트에서 정규화하세요. 엔진은 `==` 만 압니다.",
  "The script for target `{target}` ended in an exception: {path}\n{detail}": "target `{target}` 의 스크립트가 예외로 끝났습니다: {path}\n{detail}",
  "No such file: {path}\nWritten as: {raw}. Check the path, and check the value of any environment variable it references.": "파일이 없습니다: {path}\n원본: {raw}. 경로가 맞는지, 참조한 환경변수 값이 맞는지 확인하세요.",
  "Cannot load the script as a module: {path}\nA node script must be a single `.py` file.": "스크립트를 모듈로 읽을 수 없습니다: {path}\n노드 스크립트는 `.py` 파일 하나여야 합니다.",
  "Cannot load the library as a module: {path}\nA library must be a single `.py` file.": "라이브러리를 모듈로 읽을 수 없습니다: {path}\n라이브러리는 `.py` 파일 하나여야 합니다.",
  "Loading the library raised: {path}\n{detail}\nImporting a library must have no side effect — keep function definitions only.": "라이브러리를 로드하다 예외가 났습니다: {path}\n{detail}\n라이브러리는 import 만으로 부작용이 없어야 합니다 — 함수 정의만 두세요.",
  "Loading the script raised: {path}\n{detail}\n": "스크립트를 로드하다 예외가 났습니다: {path}\n{detail}\n",
  "It blew up at module top level (imports, constant computation …). Importing a node script must have no side effect — put the actual work inside `runNode(args)`.": "모듈 최상위(import·상수 계산 등)에서 터진 것입니다. 노드 스크립트는 import 만으로 부작용이 없어야 하고, 실제 동작은 `runNode(args)` 안에 두세요.",
  "`{namespace}` only ever holds **the library slots the node wired** — this script has no such wiring.\nAdd `\"libraries\": { \"<slot>\": \"${ref.lb_...}\" }` to the node JSON (an absolute path works too). At registration time `LNT-LIB-001` points at the same thing.": "`{namespace}` 에는 **노드가 배선한 라이브러리 슬롯만** 들어옵니다 — 지금 이 스크립트에는 그 배선이 없습니다.\n노드 JSON 에 `\"libraries\": { \"<슬롯>\": \"${ref.lb_...}\" }` 를 넣으세요 (절대경로도 됩니다). 등록 시점이라면 `LNT-LIB-001` 이 같은 것을 짚습니다.",
  "Module not found: `{module}`. **Importing a sibling file does not work** — the directory the script sits in is not on `sys.path`, and registration copies the script file alone into the registry, so the file next to it does not come along.\nShared logic goes one of three ways: (1) **register it as a library and let the node wire it** — `lintomata library add <file>`, then `\"libraries\": { \"<slot>\": \"${ref.lb_...}\" }` in the node JSON and `from lintomata_lib import <slot>` in the script (this is the way for project-specific decision logic). (2) Instead of sharing the function, **reuse the node that makes that decision**. (3) If it is generic third-party code, make it a small package and install it into the lintomata environment — `uv tool install lintomata --with <package>`, then declare it in the PEP 723 header so it is checked at registration time.": "못 찾은 모듈: `{module}`. **형제 파일 import 는 되지 않습니다** — 스크립트가 있는 디렉터리는 `sys.path` 에 없고, 등록하면 스크립트 파일 하나만 등록소로 복사되므로 옆 파일은 따라오지 않습니다.\n공용 로직은 셋 중 하나로 풉니다: (1) **라이브러리로 등록해 노드가 배선**한다 — `lintomata library add <파일>` 후 노드 JSON 에 `\"libraries\": { \"<슬롯>\": \"${ref.lb_...}\" }`, 스크립트에서 `from lintomata_lib import <슬롯>` (프로젝트 고유 판정 로직은 이쪽입니다). (2) 판정 함수를 공유하는 대신 **그 판정을 하는 노드를 재사용**한다. (3) 범용 서드파티라면 작은 패키지로 만들어 lintomata 환경에 설치한다 — `uv tool install lintomata --with <패키지>` 후 PEP 723 헤더에 선언하면 등록 시점에 확인됩니다.",
  "This value cannot be read as a dataclass: {value} ({type})\nValues travelling between nodes must be dataclasses declared by the script (`schema.md` §7 — a composite type is always a dataclass).": "dataclass 로 볼 수 없는 값입니다: {value} ({type})\n노드 사이를 오가는 값은 스크립트가 선언한 dataclass 여야 합니다 (`schema.md` 7절 — 복합 타입은 무조건 dataclass).",
  "The script has no dataclass `{type}`: {path} ({where})\nThe type name `Args` declares and the actual class name must match.": "스크립트에 dataclass `{type}` 이 없습니다: {path} ({where})\n`Args` 가 선언한 타입 이름과 실제 클래스 이름이 같아야 합니다.",
  "There is no value to fill `{type}.{field}` with ({where}, {path})\nFields present in the incoming value: {fields}. Every declared field must be filled — there is no `Optional`, so nothing may be left empty.": "`{type}.{field}` 에 채울 값이 없습니다 ({where}, {path})\n들어온 값의 필드: {fields}. 선언한 필드는 전부 채워져야 합니다 — `Optional` 이 없으므로 비워 둘 자리가 없습니다.",
  "A non-list value arrived where `{type}` was declared: {value} ({where})": "`{type}` 자리에 리스트가 아닌 값이 왔습니다: {value} ({where})",
  "No `Args` dataclass found: {path}\nEvery node script declares a dataclass named `Args` and takes the form `runNode(args: Args)`.": "`Args` dataclass 를 찾을 수 없습니다: {path}\n모든 노드 스크립트는 `Args` 라는 이름의 dataclass 를 선언하고 `runNode(args: Args)` 형태여야 합니다.",
  "`Args` has an unknown field: {field} ({path})\nThe fields of `Args` are `input` / `params` / `state`, nothing else.": "`Args` 에 모르는 필드가 있습니다: {field} ({path})\n`Args` 의 필드는 `input` / `params` / `state` 뿐입니다.",
  "`Args.input` is declared but no upstream value arrived: {path}\nEither add an `inputs` wiring to this node in the pipeline, or — if the node takes no input — delete the `input` field from `Args` (declare only what you use).": "`Args.input` 을 선언했는데 앞단 값이 없습니다: {path}\n파이프라인의 이 노드에 `inputs` 배선을 넣거나, 입력을 안 쓰면 `Args` 에서 `input` 필드를 지우세요 (쓰는 것만 선언합니다).",
  "No entry point `{entrypoint}(args)`: {path}\nThe entry point name is fixed and the same for every node type.": "진입점 `{entrypoint}(args)` 가 없습니다: {path}\n진입점 이름은 전 노드 타입 공통으로 고정입니다.",
  "`{entrypoint}` raised: {path}\n{detail}\nA script exception is not a violation, it is an **error** — nothing differs from the plan, the check itself could not run. Fix the script.": "`{entrypoint}` 가 예외를 냈습니다: {path}\n{detail}\n스크립트 예외는 위반이 아니라 **오류**입니다 — 기획과 다른 것이 아니라 검사 자체가 못 돈 것입니다. 스크립트를 고치세요.",
  "the upstream node's output": "앞단 노드의 출력이",
  "the return type": "반환 타입",
  "the value emitted by `returnResult()`": "`returnResult()` 로 내보낸 값이",
  "{where} (`{type}`) does not match the actual value: {path}\n{detail}\n{hint} must have the same fields and types as the declared dataclass. If the declaration is what is wrong rather than the value, fix the dataclass in the script.": "{where}(`{type}`) 과 실제 값이 맞지 않습니다: {path}\n{detail}\n{hint} 선언된 dataclass 와 같은 필드·타입을 가져야 합니다. 값이 아니라 선언이 틀렸다면 스크립트의 dataclass 를 고치세요.",
  "The Reckon output has no `{passed}: bool` field (fields: {fields})\nA Reckon is the node that produces a decision, so its return dataclass must carry `{passed}: bool` for the engine to read pass/violation off it. Emit the violation text as `{message}: str` and the rule name as `{rule}: str` alongside it and they land in the report verbatim. **The script decides; the engine only reads that conclusion.**": "Reckon 의 출력에 `{passed}: bool` 필드가 없습니다 (필드: {fields})\nReckon 은 판정을 내놓는 노드이므로 반환 dataclass 에 `{passed}: bool` 을 두어야 엔진이 통과/위반을 읽을 수 있습니다. 위반 설명은 `{message}: str`, 규칙 이름은 `{rule}: str` 로 함께 내보내면 리포트에 그대로 실립니다. **판단은 스크립트가 하고 엔진은 그 결론을 읽기만 합니다.**",
  "Differs from the plan. Verdict: {verdict}\nPut a `{message}: str` on the Reckon's return dataclass and its text lands right here instead.": "기획과 다릅니다. Verdict: {verdict}\nReckon 의 반환 dataclass 에 `{message}: str` 을 두면 이 자리에 그 문구가 그대로 실립니다.",
  "Cannot read the {label} file: {path} ({detail})\nA {label} file must be UTF-8 JSON.": "{label} 파일을 읽을 수 없습니다: {path} ({detail})\n{label} 파일은 UTF-8 JSON 이어야 합니다.",
  "The top level of the {label} file is not an object: {path}\nFollow the {label} structure in `schema.md` exactly.": "{label} 파일의 최상위가 객체가 아닙니다: {path}\n`schema.md` 의 {label} 구조를 그대로 따르세요.",
  "The transition's `delay` is a boolean: {value}\n`delay` must be an integer number of milliseconds, or a `${config.X}` / `${env.X}` reference.": "전이의 `delay` 가 참/거짓입니다: {value}\n`delay` 는 밀리초 정수이거나 `${config.X}`·`${env.X}` 참조여야 합니다.",
  "The transition's `delay` did not expand to an integer: {raw} → {value}\n`delay` is an integer number of milliseconds. If it comes from `${config.X}`, declare that config's `type` as `int` and fill an integer in from the Spec. If it comes from `${env.X}`, that environment variable's value must look like an integer.": "전이의 `delay` 가 정수로 풀리지 않았습니다: {raw} → {value}\n`delay` 는 밀리초 정수입니다. `${config.X}` 로 받는다면 그 config 의 `type` 을 `int` 로 선언하고 Spec 에서 정수를 채우세요. `${env.X}` 로 받는다면 그 환경변수 값이 정수 형태여야 합니다.",
  "The transition's `delay` is negative: {value}\n`delay` is how many milliseconds to wait before the transition, so it must be 0 or more.": "전이의 `delay` 가 음수입니다: {value}\n`delay` 는 전이까지 기다릴 밀리초이므로 0 이상이어야 합니다.",
  "script (`sc_`)": "스크립트(`sc_`)",
  "library (`lb_`)": "라이브러리(`lb_`)",
  "node (`nd_`)": "노드(`nd_`)",
  "pipeline (`pl_`)": "파이프라인(`pl_`)",
  "Spec (`sp_`)": "Spec(`sp_`)",
  "one of {kinds}": "{kinds} 중 하나",
  "{id} (unknown prefix)": "{id} (모르는 접두)",
  "{kind} ({id})": "{kind} ({id})",
  "Not in `${ref.<id>}` form: {value}\nA reference must be the whole value, one reference and nothing else (e.g. ${ref.nd_e5f6a7b8}).": "`${ref.<id>}` 형태가 아닙니다: {value}\n참조는 값 전체가 참조 하나여야 합니다 (예: ${ref.nd_e5f6a7b8}).",
  "Written as: {raw}": "원본: {raw}",
  "Never closed (written as: {raw})": "닫히지 않았습니다 (원본: {raw})",
  "The path is empty.": "경로가 비어 있습니다.",
  "Current target: {target}": "현재 target: {target}",
  "The engine only provides {names}.": "엔진이 주는 것은 {names} 뿐입니다.",
  "The rule table has duplicate ids": "규칙 테이블에 중복 id 가 있습니다",
  "Unknown rule id: {id}. Only the ids listed in the table in `rules.md` §2 can be used.": "등록되지 않은 규칙 id 입니다: {id}. 규칙 id 는 `rules.md` 2절 테이블에 있는 것만 쓸 수 있습니다.",
  "No value was given for placeholders of rule {id}: {missing}. This rule requires the placeholders {slots} (`rules.Rule.slots`). Pass the values in the `fields` dict.": "규칙 {id} 의 자리표시자 값이 주어지지 않았습니다: {missing}. 이 규칙이 요구하는 자리표시자는 {slots} 입니다 (`rules.Rule.slots`). 값은 `fields` 딕셔너리로 넘깁니다.",
  "`LINTOMATA_HOME` is not an absolute path: {path}\nA registry path must not depend on cwd. Point it at an absolute path such as `/home/me/.lintomata` or `~/.lintomata`.": "`LINTOMATA_HOME` 이 절대경로가 아닙니다: {path}\n등록소 경로는 cwd 와 무관해야 합니다. `/home/me/.lintomata` 나 `~/.lintomata` 처럼 절대경로로 지정하세요.",
  "The registry does not know this kind: {kind}\nAvailable kinds: {kinds}": "등록소가 모르는 종류입니다: {kind}\n쓸 수 있는 종류: {kinds}",
  "The file to register is not UTF-8: {path} ({reason}, byte {offset})\nOnly `.py` scripts and `.json` documents can be registered, and both must be UTF-8. Re-encode the file as UTF-8 and register it again.": "등록할 파일이 UTF-8 이 아닙니다: {path} ({reason}, byte {offset})\n등록 대상은 `.py` 스크립트와 `.json` 문서뿐이고 둘 다 UTF-8 이어야 합니다. 파일 인코딩을 UTF-8 로 바꿔 다시 등록하세요.",
  "The registry index is not UTF-8: {path} ({reason}, byte {offset})\n`registry.json` is damaged. Do not hand-edit it — rebuild the registry.": "등록소 인덱스가 UTF-8 이 아닙니다: {path} ({reason}, byte {offset})\n`registry.json` 이 손상됐습니다. 손으로 고치지 말고 등록소를 다시 만드세요.",
  "Cannot read the registry index: {path} ({detail})\n`registry.json` is damaged. Do not hand-edit it — rebuild the registry.": "등록소 인덱스를 읽을 수 없습니다: {path} ({detail})\n`registry.json` 이 손상됐습니다. 손으로 고치지 말고 등록소를 다시 만드세요.",
  "No such id in the registry: {id}\nIt was deleted, or it is a typo. Check with `lintomata <kind> list` (prefix `sc_`=script `lb_`=library `nd_`=node `pl_`=pipeline `sp_`=Spec).": "등록소에 없는 id 입니다: {id}\n삭제됐거나 오타입니다. `lintomata <종류> list` 로 확인하세요 (접두 `sc_`=스크립트 `lb_`=라이브러리 `nd_`=노드 `pl_`=파이프라인 `sp_`=Spec).",
  "No such id in the registry: {id}\n`update` only replaces something already registered. For a new one, use `add`.": "등록소에 없는 id 입니다: {id}\n수정은 이미 등록된 것에만 됩니다. 새 것이면 `add` 를 쓰세요.",
  "No such id in the registry: {id}\nIt is already deleted, or it is a typo. Check with `lintomata <kind> list`.": "등록소에 없는 id 입니다: {id}\n이미 삭제됐거나 오타입니다. `lintomata <종류> list` 로 확인하세요.",
  "No such registry file: {path}\nThe index has it but the file itself is gone. Remove the entry and register it again.": "등록소 파일이 없습니다: {path}\n인덱스에는 있는데 실제 파일이 사라졌습니다. 삭제 후 재등록하세요.",
  "The registry file is not UTF-8: {path} ({reason}, byte {offset})\nIt looks like the registry file was edited directly after registration. Fix the original as UTF-8 and register it again with `update`.": "등록소 파일이 UTF-8 이 아닙니다: {path} ({reason}, byte {offset})\n등록소 파일이 등록 이후에 직접 수정된 것으로 보입니다. 원본을 UTF-8 로 고쳐 `update` 로 다시 등록하세요.",
  "{id} {kind}": "{id} {kind}",
  "{detail} ({why})": "{detail} ({why})",
  "Cannot read the test file: {path} ({detail})\n`<node file>.test.json` must be a UTF-8 JSON file.": "테스트 파일을 읽을 수 없습니다: {path} ({detail})\n`<노드파일>.test.json` 은 UTF-8 JSON 파일이어야 합니다.",
  "The test file is not JSON: {path} ({detail})\nFollow the `<node file>.test.json` structure in `schema.md` §14.": "테스트 파일이 JSON 이 아닙니다: {path} ({detail})\n`<노드파일>.test.json` 의 구조는 `schema.md` 14절을 따르세요.",
  "The top level of the test file is not an object: {path} ({type})\nIt must have the form `{\"node\": ..., \"cases\": [...]}`.": "테스트 파일의 최상위가 객체가 아닙니다: {path} ({type})\n`{\"node\": ..., \"cases\": [...]}` 형태여야 합니다.",
  "The top level of the node file is not an object: {path} ({type})": "노드 파일의 최상위가 객체가 아닙니다: {path} ({type})",
  "`{key}` must be the only key of that object: {keys} ({path})\nA `bytes` field is written only as `{\"{key}\": \"<absolute path>\"}`.": "`{key}` 는 그 객체의 유일한 키여야 합니다: {keys} ({path})\n`bytes` 필드는 `{\"{key}\": \"<절대경로>\"}` 로만 씁니다.",
  "The value of `{key}` is not a path string: {value} ({path})": "`{key}` 의 값이 경로 문자열이 아닙니다: {value} ({path})",
  "Cannot read the fixture file: {path} ({detail})\nThe file `{key}` points at must exist.": "fixture 파일을 읽을 수 없습니다: {path} ({detail})\n`{key}` 가 가리키는 파일이 실재해야 합니다.",
  "case: {name}\ninput: {input}\nreturned: {output}": "케이스: {name}\n입력: {input}\n반환: {output}",
  "There is no pair of cases with the same `input` and different `params` (of {total} cases, {readable} yield a readable decision).": "`input` 이 같고 `params` 만 다른 케이스 쌍이 없습니다 (케이스 {total}개 중 판정을 읽을 수 있는 것 {readable}개).",
  "`{left}` and `{right}` have different `params`, yet both decide {verdict}.\nparams: {left_params} vs {right_params}": "`{left}` 과 `{right}` 은 `params` 가 다른데 판정이 둘 다 {verdict}입니다.\nparams: {left_params} vs {right_params}",
  "The type expression is empty. Write `int` `float` `str` `bool` `bytes` `list[T]` or the name of a declared dataclass.": "타입 표기가 비어 있습니다. `int` `float` `str` `bool` `bytes` `list[T]` 또는 선언한 dataclass 이름을 쓰세요.",
  "Cannot parse the type expression {expr} (trailing text after character {pos}). Write a name and brackets only, as in `list[Button]`.": "타입 표기 {expr} 를 해석할 수 없습니다 ({pos} 번째 글자 뒤가 남았습니다). `list[Button]` 처럼 이름과 대괄호만으로 쓰세요.",
  "{type} is not a `list[T]`, so it has no element type.": "{type} 는 `list[T]` 가 아니므로 원소 타입이 없습니다.",
  "No type name found at character {pos} of the type expression {expr}. Write `int` `float` `str` `bool` `bytes` `list[T]` or the name of a declared dataclass.": "타입 표기 {expr} 의 {pos} 번째 위치에서 타입 이름을 찾지 못했습니다. `int` `float` `str` `bool` `bytes` `list[T]` 또는 선언한 dataclass 이름을 쓰세요.",
  "The brackets in the type expression {expr} are never closed. Write it as `list[Button]`.": "타입 표기 {expr} 의 대괄호가 닫히지 않았습니다. `list[Button]` 처럼 쓰세요.",
  "The dataclass `{name}` is declared twice with different definitions inside the same script ({origin}). Within one script the same name must have the same field set — if it is a different concept, give it a different name.": "dataclass `{name}` 가 같은 스크립트({origin}) 안에서 서로 다른 정의로 두 번 선언됐습니다. 한 스크립트 안에서 같은 이름은 같은 필드 구성이어야 합니다 — 다른 개념이면 이름을 다르게 두세요.",
  "The dataclass `{name}` has the same field name twice. Field names must be unique within a dataclass.": "dataclass `{name}` 에 같은 이름의 필드가 두 번 있습니다. 필드 이름은 dataclass 안에서 유일해야 합니다.",
  "Cannot resolve the field type `{type}` of {where}. The types you can use are `int` `float` `str` `bool` `bytes` `list[T]` and the dataclasses declared by **the same script** — a dataclass from another script cannot be referenced by name.": "{where} 의 필드 타입 `{type}` 를 해석할 수 없습니다. 쓸 수 있는 타입은 `int` `float` `str` `bool` `bytes` `list[T]` 와 **같은 스크립트가** 선언한 dataclass 뿐입니다 — 다른 스크립트의 dataclass 는 이름으로 참조할 수 없습니다.",
  "The TypeRegistry was queried before it was normalized. Call every `register()` first, then `normalize()` once, then query.": "TypeRegistry 를 정규화하지 않고 조회했습니다. `register()` 를 전부 부른 뒤 `normalize()` 를 한 번 부르고 조회하세요.",
  "The dataclass {key} is not registered. Only dataclasses declared by a script can be used as types, and the lookup key is `(origin, name)`.": "dataclass {key} 가 등록되어 있지 않습니다. 스크립트가 선언한 dataclass 만 타입으로 쓸 수 있고, 조회 키는 `(origin, name)` 입니다.",
  "{owner} says {owner_type} / {member} says {member_type}": "{owner} 는 {owner_type} / {member} 는 {member_type}"
}
