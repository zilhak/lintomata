"""노드 단위테스트 하네스 (`schema.md` 14절).

**기본 제공 타입검사 + 사용자 커스텀 단위테스트.** 별개 기능이 아니라 **같은 실행의
두 층위**다. 입력 fixture 만 주면 돌려서 타입이 맞는지(기본 제공), 기대 출력까지 주면
값이 맞는지(커스텀).

| | 스크립트를 | 잡는 것 |
|---|---|---|
| **등록/수정 시 검사** (`checks`) | **안 돌린다** | 선언·형식·금지 패턴 |
| **단위테스트** (여기) | **돌린다** | 선언한 대로 실제로 동작하는가 |

**★ Perceive 스크립트 검사 카테고리를 이것이 흡수한다.** `.test.json` 이 하는 일이
정확히 *"이 HTML 을 주면 버튼을 3개로 인식하는가"* 이고, 그게 곧 Perceive 스크립트가
개념을 제대로 인식하는가를 묻는 것이다. **별도 파이프라인을 짤 필요가 없다.**

⚠ stub. Step 4 에서 구현한다.
"""

from __future__ import annotations

__all__: list[str] = []
