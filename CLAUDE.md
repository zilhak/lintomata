# strictler

## 이 프로젝트는 무엇인가

**기획대로 돌아가는지를 검사하는 도구.**

이 프로그램이 보는 것은 오직 하나 — **"잘 되는지"**.
그리고 이 **"잘 된다"는 곧 기획 데이터**다. 기획이 정의한 대로 돌아가면 통과, 아니면 실패.

**QA의 프로그램화, lint의 인간 버전.**

## ★ 이건 lint 다. 실행 파이프라인이 아니다. — 사고방식의 기준

**"실패"도 하나의 정상적인 상태다.**
위반을 찾아낸 것은 사고가 아니라 **lint 가 제 일을 한 것**이다. 실패는 **산출물**이지 수습 대상이 아니다.

→ **"실패했을 때 무엇을 해야 하나"라는 사고를 하지 않는다.**
  복구·재시도·되돌아가기·대체 경로·에러 핸들링 — 전부 실행 엔진의 사고방식이고 여기 해당 없다.
  실패하면 그 지점에서 진행하지 않는다. 그게 전부다.

**반드시 세 가지를 구분한다:**

| | 무엇 | 성격 |
|---|---|---|
| **위반** | 기획과 다르다 | **정상 결과.** 리포트에 담긴다 |
| **not run** | 앞단 실패의 여파로 도달 불가가 됐다 | **정상 결과.** 통과와 구분해 보고한다 |
| **오류** | 스크립트 예외, 계약 위반, 경로 없음, 환경변수 미정의 | **비정상.** lint 결과가 아니라 **도구가 못 돈 것** |

앞의 둘은 그냥 결과다. **고쳐야 할 것은 세 번째뿐이다.**
이 구분이 흐려지면 위반을 오류처럼 다루거나(불필요한 복구 로직), 오류를 위반처럼 다루게 된다(거짓 리포트).

**조건 분기도 이 사고를 따른다.** "실패했으니 다른 경로로" 같은 것은 없다.
분기는 **스크립트가 그냥 `input` 을 반환**하는 것으로 표현한다 — *"앞단 결과가 이러면 나는
아무것도 안 하고 그대로 흘려보낸다."* **엔진에는 skip 개념이 없다.** 엔진은 평소대로
input/output 타입이 맞는지만 검사하고, 타입을 맞추는 건 스크립트의 책임이다.
→ 상태 전이 조건에 표현식 언어도, `skipWhen` 같은 필드도 필요 없다. `transitions` 는 **시간**만 다룬다.

**실패는 최대한 수집하고, 여파는 not run 이다.** 한 번의 실행에서 확인 가능한 실패를 전부 모은다.
한 항목이 실패해도 다른 항목은 전부 돈다. 단 실패 지점의 뒷단을 억지로 이어가면 **실패가 전파되어**
원인이 뭉개지므로, 그 노드에 **데이터로 의존하는 노드**와 **그 노드가 바꿔야 할 상태에 조건이 걸린 노드**를
전부 `not run` 으로 표기한다. → **노드를 전수 검사해서** 도달 불가가 된 것을 싹 다 바꾼다.

## 용도 — 두 가지

1. **기획 → 프로그램 검증** — 준비된 기획대로 돌아가는지 검사 (QA 자동화 본연)
2. **프로그램 ↔ 프로그램 동일성 검증** — 리뉴얼·마이그레이션 결과가 원본과 같은지

용도 2 는 **동일성 검증 전용 파이프라인을 별도로 작성**해서 한다.
이 파이프라인은 **여러 소스를 같은 파이프라인에 태우고 중간값이 동일한지** 점검한다.
**대상 개수에 제한이 없다** — 둘도 되고 열도 된다.
**위반 판정은 목록 전부가 같은 값을 뱉느냐**이지 짝지어 비교하는 것이 아니다.
스크립트의 관점이 *"특정 값이 있나"* 에서 **"여러 대상이 동일한 값을 가지나"** 로 바뀔 뿐이다.

→ A 와 B 는 인식 스크립트가 다르다. 그게 설계다 — 각자 개념 레벨까지 올린 뒤 그 층위에서 비교하므로
  **HTML 이 완전히 달라도 개념이 같으면 통과**한다.

**파이프라인은 두 종류다. 비교 파이프라인은 속성과 실행 엔진을 따로 둔다.**

| 파이프라인 종류 | 하는 일 | Reckon |
|---|---|---|
| **값 검증 파이프라인** | 기획대로인지 검사 | 필요 |
| **비교 파이프라인** | 노드 출력이 대상 간 동일한지 비교 (**엔진이 수행**) — 입력 N개 | **불필요** |

**→ Reckon 은 필수가 아니다.** 비교 파이프라인에선 Verdict 를 엔진이 만든다.
"내장 동작 없음" 원칙과 충돌하지 않는다 — **동등 비교는 도메인 지식이 아니라 일반 연산**이다.

**비교는 `Percept` 층에서 한다.** `Sensum` 은 비교 대상이 아니다 —
구조 리팩터링을 하면 HTML 은 완전히 달라지고, 그걸 잡으려는 게 아니라 **개념 층이 같은지**를 묻는 것이다.

**"동일하다"는 정말로 동일하다는 뜻이다.** 허용 오차도 무시 필드도 엔진에 두지 않는다.
**정규화는 비교용 데이터를 출력하는 노드의 스크립트가 알아서 한다** — 좌표 반올림, 타임스탬프 제거 등
**동일하지 않을 데이터는 스크립트가 걸러서 내보낸다.** 무엇을 무시해도 되는지는 도메인 지식이고,
도메인 지식은 언제나 스크립트 쪽에 있다. 엔진은 `==` 만 안다.
→ 동일성 비교용 Perceive 스크립트는 의도 검증용과 별개로 두는 것이 자연스럽다.

**비교 파이프라인은 실행과 동시에 결과 리포트를 쌓는다** — 무엇이 어디서 어떻게 달랐는지.
리포트 **출력 위치는 Spec 의 `plan` 항목**에 지정하고, 파이프라인이 그쪽에 쌓는다.
(파이프라인 실행 전체 로그는 기본적으로 적히며 이것과 별개)
→ "증거 캡처는 하지 않는다"와 모순되지 않는다. 그건 *위반 후 사후 수습*을 거부한 것이고,
  이건 **실행과 동시에 쌓는 산출물**이다. 성격이 다르다.

**~~프로그램 → 기획 추출~~ 은 폐기했다.** 형상에서 기획을 역으로 뽑는 방법이 없다.
→ 함께 사라진 것: provenance(`authored`/`extracted`) 구분, VRT 퇴화 위험,
  그리고 **"스키마가 기계 생성 가능해야 한다"는 제약**. 스키마는 이제 **AI 가 쓰기 좋기만 하면 된다.**

## 기획 데이터

- **strictler 가 "기획 표현 스키마"를 정의한다.**
- **사용하는 쪽이 그 스키마에 맞춰 기획 데이터를 미리 준비해 둔다.**
- 기획 데이터는 **언제나 입력**이다. 도구가 만들어내는 경로는 없다.
  기획 데이터가 없으면 검사할 것도 없다 — 전제 조건이지 결함이 아니다.

**이 스키마가 제품의 본체다.** 스키마가 표현하지 못하는 기획은 검사할 수 없으므로,
스키마의 표현력이 곧 제품 능력의 상한이다.

## 개념 인식은 사용자가 정의한다

기획 스키마만 던져주고 끝이 아니다. **"무엇이 버튼인가"를 판정하는 스크립트를
사용자가 직접 만들어 스키마에 경로로 첨부한다.**

`<button>` 이 있다고 그게 버튼인 게 아니다. 누를 수 있는 배경화면일 수도, 실제 버튼일 수도 있다.
마크업만 보고는 판단이 안 되고, 이건 QA 이므로 이 구분이 틀리면 검사 전체가 무의미하다.
개념 인식은 프로젝트마다 다른 도메인 지식이라 도구가 일반해를 가질 수 없다 → **사용자에게 위임한다.**

그래서 기획 스키마는 **파이프라인 선언**이 된다. Tekton 같은 느낌 —
선언적 DAG 이고, 각 단계의 실행 단위는 사용자가 공급한다.

| | 무엇 |
|---|---|
| **strictler 가 제공** | 스키마, DAG·상태머신 실행 엔진, 타입 계약 검증, 노드 단위테스트 하네스, 리포터 |
| **사용자 쪽이 제공** | **모든 노드의 스크립트** — 내장 동작은 하나도 없다. 실제 작성은 AI 가 한다 |

Perceive 스크립트는 규칙마다가 아니라 **프로젝트마다 한 번** 만든다. 일회용 스크립트가 아니라 프로젝트 자산이다.

### Perceive 스크립트 검증도 하나의 카테고리

Perceive 스크립트가 틀리면 검사 전체가 조용히 무의미해진다. 그래서 **Perceive 스크립트 검증을 별도 검사 카테고리로 둔다.**

| 카테고리 | 검사 대상 | 묻는 것 | 어디서 |
|---|---|---|---|
| 프로그램 검사 | Target | 기획대로 돌아가는가 | Spec 실행 |
| Perceive 스크립트 검사 | Perceive 스크립트 | 개념을 제대로 인식하는가 | **노드 단위테스트** |

**별도 파이프라인이 필요 없다.** 노드 단위테스트가 하는 일이 정확히 *"이 HTML 을 주면 버튼을 3개로
인식하는가"* 이므로 그대로 이 카테고리다.

### 노드 단위테스트 — 기본 타입검사 + 사용자 커스텀

별개 기능이 아니라 **같은 실행의 두 층위**다. 입력 fixture 만 주면 **돌려서 타입이 맞는지**(기본 제공),
기대 출력까지 주면 **값이 맞는지**(커스텀). `<노드파일>.test.json` 에 둔다.

- **등록 검사는 스크립트를 안 돌린다**(형식·선언·금지 패턴). **단위테스트는 돌린다**(선언대로 동작하는가)
- Action 은 `input == output` 이므로 **값 동일성을 자동 검사**한다 — 기대값이 곧 입력
- Reckon 은 **기댓값 반응성**을 본다 — `input` 이 같고 `params` 만 다른 통과/위반 쌍의 판정이 같으면
  기댓값을 안 쓰는 것이다. 정적으로는 못 잡는 하드코딩을 여기서 잡는다
- **결정성 검사(2회 실행 비교)는 하지 않는다** — AI 를 부르는 Perceive 가 당연히 실패한다

**사용자 코드 실행에 샌드박싱은 하지 않는다.** ESLint 플러그인·vite 플러그인·jest transform 이
전부 사용자 코드를 그냥 로드해 실행한다. lint 계열의 표준 신뢰 모델을 그대로 따른다.

⚠ 스키마에 절차가 들어오지만 이건 **검사 파이프라인의 절차**지 **사용자 시나리오의 절차**가 아니다.
"로그인하고 클릭하고 기다린다"를 쓰는 순간 E2E 프레임워크가 된다.

## 원칙 — 철저히 인간의 관점에서 본다

보는 것은 사람이 보는 것뿐이다:
- 버튼이 몇 개인가, 버튼이 어디에 있는가
- 메뉴가 몇 개인가, 정의된 순서대로 놓여 있는가
- 눌렀을 때 떠야 할 것이 뜨는가

따라서 **HTML 구조가 아무리 파멸적이어도 기획대로 보이고 동작하면 통과다.**

## 유비쿼터스 언어 — 네 단계

사람이 보고 판단하는 과정을 그대로 옮겼다. **인지 과정의 어휘를 쓴다.**

| 단계 | 노드 | 산출물 | 하는 일 |
|---|---|---|---|
| ① | **Vantage** | `Scene` | 어디서 볼 것인가 — 관측 지점·대상 확보 |
| ② | **Sense** | `Sensum` | 감각 입력 — 해석 없는 원시 데이터 (HTML, 파일목록…) |
| ③ | **Perceive** | `Percept` | 지각 — 의미 부여 (버튼, 메뉴…) |
| ④ | **Reckon** | `Verdict` | 판정 — 기획과 대조 |
| — | **Action** | (상태 변화) | **행위** — 클릭·입력. 네 단계 어디에도 속하지 않는 직교 노드 |

**②와 ③의 이름이 핵심** — 인지과학의 감각(sensation)/지각(perception) 구분 그대로다.
전체는 **DAG** 위에 배치된다(Tekton 식).

### ★ 모든 노드의 본체는 스크립트다

**특정 노드만 스크립트인 게 아니다.** 다섯 타입 전부 본체가 스크립트다.
→ **strictler 에 내장된 동작이 하나도 없다.** input/output 타입 계약과 단위테스트가 다섯 타입에 균일하게 적용된다.

**노드 타입은 스크립트의 형식까지 제한하고, strictler 가 타입별 스크립트 검사기를 제공한다.**
이게 여러 성질을 구조적으로 강제한다:
- **Reckon 은 기댓값을 Spec 에서 파라미터로 받아야 한다** — `Args.params` 에 기댓값 필드 필수.
  스크립트에 기댓값을 하드코딩하면 **기획 파일이 껍데기가 되고**, 기획을 고쳐도 판정이 안 바뀌며,
  "같은 기획을 A/B 에 돌린다"(용도 2)가 성립하지 않고, "기획대로면 통과"라는 정의 자체가 무너진다.
  형식 제한이 이 자리를 없앤다.
- **AI 가 스크립트를 잘못 썼을 때 돌리기 전에 잡아 자기 수정 신호를 준다** — AI 저작 워크플로우의 안전망.

### Action — 행위는 직교 노드. input == output

클릭·입력은 지각이 아니다. Perceive 는 `Sensum → Percept` 변환일 뿐이므로 행위를 담을 수 없다.
→ **행위를 별도 노드로 뗀다.** 상하위 개념 없이 노드 사이 어디에나 끼워 넣는다.
→ `Percept` 를 입력으로 받을 수 있어야 한다 (폼을 검출해야 어디에 입력할지 알 수 있으므로).

**Action 은 특수 노드로, input 과 output 이 동일하다.**
`X ──▶ Action ──▶ Y` 는 실은 **`X ──▶ Y` 인데 그 사이에 Action 이 낀 형상**이다 —
상단과 하단이 하나의 노드고, Action 은 중간에서 **부작용만** 일으킨다. 데이터 변환은 없다.
→ 타입 검사 관점에서 **Action 은 투명하다.** 그래서 "어디에나 끼워 넣는다"가 타입 체계와 충돌하지 않는다.
→ 클릭 결과를 후속 노드가 알아야 하면 **후속 Sense 가 다시 관측한다.**

`Action` 은 다른 도구에도 흔한 단어지만 **그대로 쓴다.** 아래 명명 원칙 참조 —
이 노드의 정체성이 말 그대로 action 이므로 혼란이 생기지 않는다.

### 타이밍 = 파이프라인 상태머신 + 노드 condition

타이밍을 엣지나 노드 속성으로 흩뿌리지 않는다.
**실행 파이프라인 자체가 상태머신**이고, **각 노드는 실행 조건에 condition 체크가 가능**하다.
(Tekton `when` 과 같은 자리)
**"API 캡처 시작 … 종료" 같은 구간형은 Sense 두 개다.** 시작 Sense 와 종료 Sense.
**그 안에서 뭐가 이루어지는지 엔진은 관여하지 않는다** — playwright 가 뭘 하는지 엔진은 모른다.
→ Sense 는 서로 **선형으로 이어질 수 있다.** 시작 Sense 는 참조 정보를 반환하고,
   종료 Sense 는 외부의 특정 조건을 걸고 있다. 특수 문법이 아니라 그냥 일반 노드 둘이다.

**상태 이름은 파이프라인마다 다르다** (`idle/active` vs `wait/run`).
노드는 자기 어휘로 필요 상태를 선언하고, **파이프라인 config 가 이름 매핑을 갖는다.**
→ 노드를 파이프라인 간에 그대로 재사용할 수 있다.
→ 로드 시점에 **매핑 누락·존재하지 않는 상태 참조를 잡아야 한다** (자기 검증적 스키마).

**상태 전이는 파이프라인 구동 엔진(별도 런타임 모듈)이 관리한다.** 상태는 파이프라인에 있으므로 파이프라인이 관리한다.
노드는 상태를 **읽기만** 하고(condition), 결과만 반환한다. 전이는 런타임이 수행한다.
→ 재사용 노드가 남의 파이프라인 상태를 오염시킬 수 없고, 상태 이름 매핑이 읽기 전용이라 단순해진다.
→ **파이프라인 정의에 전이 규칙 섹션이 필요하다.**

### 노드 계약의 구성요소

| 요소 | 내용 |
|---|---|
| input 타입 | 무엇을 받는가 |
| output 타입 | 무엇을 내놓는가 |
| 필요 상태 id | 어떤 상태를 참조하는가 (파이프라인 config 에서 매핑) |
| 파라미터 | Spec 에서 받는 기댓값 등 |

### 타입 체계 — 좁게 강제한다

**내장 의미 타입(`Button` 같은 것)은 없다.** 하지만 **타입 체계는 있고, 쓸 수 있는 타입을 강제해 범위를 좁힌다.**

- 쓸 수 있는 것은 **primitive**(`int` `float` `str` `bool` `bytes` `list[T]`) 와
  **노드가 사전 선언한 dataclass**, 둘뿐이다.
- **복합 타입은 무조건 `dataclass`.** C++ 의 `struct` 같은 자리. **`dict` 는 금지** — 허용하면
  dataclass 강제가 한 줄로 무의미해진다. **`None`/`Optional` 도 금지** — 부분집합 규칙과 겹쳐 판정이 흐려진다.

**★ 그래프 검사와 데이터 취급은 서로 다른 층이다.**

| 층 | 대상 | 규칙 |
|---|---|---|
| **그래프 검사 (정적)** | **선언된 정의** | input 정의 **==** output 정의. 엄격한 동일성 |
| **데이터 취급 (런타임)** | **표현** | 부분집합 관계인 것들은 **상위 큰 dataclass 하나를 같이 써버린다** |

**부분집합 병합이 타입 검사를 느슨하게 만드는 게 아니다.** 병합은 표현 층에서만 일어난다.
여분 필드가 비어 있어도 안전한 이유는, **그래프 검사가 정의 동일성을 이미 확인했으므로
그 필드를 읽는 노드는 그 필드를 채우는 노드하고만 연결되기 때문**이다.

**엔진은 구성 시점에 모든 dataclass 를 등록하고 집합 검사를 전체에 건다.**
각 dataclass 를 `(필드명, 타입)` 쌍의 **집합**으로 다루면 순서·이름/타입·중첩 재귀가 전부 자동으로 풀린다
(중첩은 위상 정렬해 바닥부터 정규화). 병합 단위는 **부분집합 격자의 연결 성분 전체를 합집합**으로.

**진입점은 `runNode(args)`, 출력은 `returnResult()` 로 이름 고정.**
`args` 의 타입은 **`Args`** 라는 고정 이름의 dataclass — 없으면 오류. **반환 타입 이름은 자유**(구조로 매칭).
`Args` 는 `input` / `params` / `state` 세 필드로 출처를 나눈다.

**★ 노드 내부는 input / output 만 맞추면 된다.**
내부에서 **AI 를 부르든 파일을 읽든 네트워크를 타든 상관하지 않는다.**
**순수함수는 강제하지 않는다** — 순수성이라는 개념 자체를 두지 않는다.
AI 를 껴서 output 을 잘못 내놓으면 **타입 계약에 걸려 그냥 에러**다. 그걸로 충분하다.

**금지는 전 노드 균일하다:**

| 금지 | 이유 |
|---|---|
| **시간 의존** | 실행 시각은 엔진이 `${state.__startedAt}` 로 준다 |
| **랜덤** | 같은 이유 |
| **직접 subprocess 호출** | `subprocess.run` / `exec` 류. **`tool` 선언분은 예외** — 그게 부가적으로 subprocess 를 띄우는 건 막지 않는다 |
| **미선언 state 참조** | `need_state` 에 없는 것을 쓰면 에러 |

그 외에는 아무것도 금지하지 않는다.
**노드 타입별로 남는 것은 금지가 아니라 형식 요구다** — 예: Reckon 의 `Args` 는 기댓값 필드를 가져야 한다.

**완벽한 정적 검사는 목표가 아니다.** `__import__("ti"+"me")` 같은 우회는 못 막는다.
**사전에 추측할 수 있는 행위만** 막고, 대신 **에러 메시지에 자연어 가이드를 넣는다** —
작성 주체가 AI 라는 전제 덕에 유효하다.

**스크립트의 선언과 Spec 의 사용은 따로다 — 통일하지 않는다.**
스크립트 내부 정의는 **능력 선언**("이거 허용해주세요"), Spec 의 파라미터는 **사용 선언**("내 검사에선
이거 쓸겁니다"). 스크립트가 인자를 3개 받을 수 있어도 어떤 Spec 은 2개만 쓸 수 있다.
`${config.X}` 라는 참조 문법을 공유할 뿐 선언 층과 사용 층은 별개다.
상태 이름 바인딩은 또 다른 층이다 — 값이 아니라 **파이프라인 상태 집합에 대한 이름 매핑**.

## Spec — JSON 으로 나타낸 단위테스트 묶음

```jsonc
// login.json
{
  "info": { "description": "이 파일이 무엇을 검사하는지", "version": "..." },
  "tool": {                                    // 외부 도구 호출 경로 전체 — 경로 + 허용 함수
    "playwright": { "path": "${env.HOME}/.playwright/playwright", "functions": ["launch"] },
    "node":       { "path": "/usr/bin/node", "functions": [] }
  },
  "plan": [ { /* 파이프라인 */ }, { /* 파이프라인 */ } ]
}
```

- `info` — 단위테스트의 `describe` 에 해당하는 메타
- `tool` — **외부 도구 호출 경로 전체를 관리한다.** 단순 경로만이 아니라 **허용 함수도 같이 적는다.**
  외부 도구는 사용자가 설치하고 경로를 여기서 받는다.
  검사는 **함수 호출 형태 전체를 정의하는 게 아니라**, **함수명 + 그 인자로 적힌 실행파일 경로**를 대조하는 정도다
- `plan` — 파이프라인 목록. **동작과 구체적 검사 방법은 전부 파이프라인 안에서 정의된다**

**`plan` 항목은 무조건 파이프라인 참조다. 인라인 금지.**

```jsonc
{
  "source": "${env.PROJECT_ROOT}/pipelines/login-flow.json",  // 파이프라인 파일 경로
  "description": "이거 뭐하려고 한건지",       // 항목별 의도 (단위테스트의 it)
  "config": { /* 파이프라인이 선언한 config 변수들의 값 */ }
}
```

**파이프라인은 독자적인 데이터 구조를 갖는 별도 JSON 파일**이고 그 안에 자기 설명을 담는다.
파이프라인이 config 변수를 **선언**하고 Spec 이 **값을 채운다.**

### 경로 규칙 — 무조건 절대 경로

**모든 경로는 절대 경로다. 단, 환경변수를 쓸 수 있다.**

```
`~` 전개  →  환경변수 전개  →  결과가 절대 경로 스타일이 아니면  →  무조건 에러
```

- 상대 경로 금지 → **cwd 의존성이 사라진다.** 어디서 실행하든 같은 동작
- 이식성은 환경변수가 담당한다 — 머신·CI 마다 값만 다르게 두면 Spec 은 그대로 커밋된다
- 환경변수 값 자체가 상대 경로여도 잡힌다 (`PROJECT_ROOT=./foo` → 에러)
- **`~` 는 허용** — 상대 경로가 아니라 cwd 와 무관하게 홈으로 결정되는 규약이므로 전개하면 절대 경로다.
  `${env.HOME}` 의 설탕 문법. (`os.path.expanduser()` 가 `~`·`~user` 를 처리)

**판정 어휘를 스키마가 가질 필요가 없다.** "위치가 맞다"를 어떻게 판정할지는
Perceive 스크립트가 좌표를 뽑고 Reckon 스크립트가 비교하며, Spec 은 기댓값만 `config` 로 넘긴다.

## 계층 — Spec / Pipeline / Node / Script

| 계층 | 내용 | 비유 |
|---|---|---|
| **Spec (기획)** | 어떤 Pipeline 을 쓰는지 + 무엇을 보장하는지(intent) | 단위테스트의 `describe` |
| **Pipeline** | 노드들의 DAG 구성 | Tekton `Task` |
| **Node** | **동작 정의** — 어떤 타입이고 어느 스크립트가 도는지 | Tekton `Step` |
| **Script** | **실제 동작 코드** | — |

**★ 네 층은 전부 별개 파일이다.** `Spec(JSON) → Pipeline(JSON) → Node(JSON) → Script(.py)`.
**여러 스크립트가 나눠 쓰는 함수는 `Library(.py)` 로 따로 등록하고 노드가 배선한다** (`docs/schema.md` 6.5절) — 형제 파일 import 는 되지 않는다.
**노드와 스크립트도 별개 파일**이다 — 노드는 *동작 정의*, 스크립트는 *실제로 동작할 코드*.

→ **노드 재사용은 별도 기능이 아니라 근본 동작**이다. 서브그래프 재사용 문법 같은 것을 둘 이유가 없다.
  ⚠ 단 이 사유는 **노드 하나의 재사용**만 다룬다 — *배선의 반복*은 **보류 안**으로 따로 있다 (`docs/schema.md` 16.5절).
→ Spec 에는 스크립트 본문이 없다. 파이프라인의 노드 항목은 **노드 파일 참조 + 배선/매핑**만 갖는다.
→ **`type` 은 노드 파일에 있다.** 노드의 성질이지 사용처의 성질이 아니다.

**무엇이 어디에 사는가** — 선언/사용 분리가 파일 경계와 그대로 겹친다:

| | 어디 | 성격 |
|---|---|---|
| `type`, `script` 경로, `description` | **노드 JSON** | 동작 정의 |
| `Args`, 출력 타입, `need_state` | **스크립트** | **능력 선언** — "이거 허용해주세요" |
| `inputs`, `params`, `states` 매핑, `when` | **파이프라인** | **사용 선언** — "내 검사에선 이거 쓸겁니다" |

### 저작 워크플로우 — 사람은 `describe` 수준만 본다

```
사람: "로그인 페이지가 잘 동작하나 체크하고 싶어"
  ↓  AI 가 구성을 자연어로 제안
사람: "그래 만들어"        ← 승인은 여기서 끝
  ↓  AI 가 타이밍·DAG 순서·스크립트를 전부 작성
```

**다섯 노드 타입 각각을 독립적으로 단위 검증할 수 있어야 한다.** AI 는 단위 동작을 검증한 뒤 연결한다.
통짜로 돌려 실패하면 AI 가 원인을 특정할 수 없으므로, 이 저작 워크플로우의 필수 조건이다.

## 명명 원칙 — 충돌 회피의 목적은 정체성 혼동 방지

**세상에 없는 단어를 만들려는 게 아니다.** 흔히 쓰이는 단어와 겹칠 때
**정체성이 달라지면 혼란이 오므로** 그걸 피하는 것이 목적이다.

| | 판단 |
|---|---|
| **정체성이 다른데 이름이 같다** | **회피한다.** `Capture`(스크린샷/패킷/regex), `Artifact`(CI), `Fact`(xUnit), `Subject`(RxJS), `Judge`(LLM-as-judge) 를 모두 피한 이유 |
| **정체성이 같은 공용 개념이다** | **그대로 쓴다.** `Action` — 이 노드는 말 그대로 action 이다. `Script` 도 마찬가지 |

제품 고유 개념에는 고유한 이름을(`Vantage`/`Sense`/`Percept`/`Reckon`),
공용 개념에는 공용 이름을(`Action`/`Script`/`Pipeline`/`config`) 쓴다.
**새 용어를 도입할 때 이 기준으로 판단할 것.**

## 관측 수단은 자유

**파싱을 해도 되고 스크린샷을 해도 된다.** 파싱은 거부 대상이 아니라 수단이다.
거부하는 것은 *파스 트리 자체를 판정 근거로 삼는 것*이다.

스크린샷은 **AI 연계용**이다 — Perceive 스크립트에서 AI 에게 지침을 내려 판정하게 하는 방식.

## 대상 범위 = 제한 없음. 엔진만 제공한다.

- **검사 대상에 제한이 없다.** 웹, 데스크톱 앱, 무엇이든.
- **특정 프로젝트에 귀속되는 lint 가 아니다.**
- **기본 검사값·내장 룰셋이 존재하지 않는다.**
- **Vantage 조차 사용자가 확장한다.** 예: `특정 프로세스 지목 → 스크린샷`.

strictler 가 파는 것은 **파이프라인 엔진과 스키마**이지 검사 내용이 아니다.

## 경계

| | 중간표현 | 판정 근거 |
|---|---|---|
| lint / 타입체커 | AST / IR | 코드의 형태 |
| DOM 셀렉터 E2E | DOM 트리 | 마크업의 형태 |
| 스냅샷 / VRT | 픽셀 | 과거의 자기 자신 |
| **strictler** | **형상(shape)** | **기획** |

strictler 는 lint 를 안 하는 도구가 아니다. **lint 의 대상을 AST 에서 형상으로,
판정 기준을 코딩 컨벤션에서 기획으로 바꾼 도구**다.

**스코프: 기존 lint 영역은 하지 않는다.** 코드 레벨 검사는 기존 lint 도구에 맡긴다.
유명 도구의 기능을 따라가거나 흉내낼 이유가 전혀 없다.

## 인터페이스 — 등록소가 중심이다

**스크립트 / 라이브러리 / 노드 / 파이프라인 / Spec 다섯 다 strictler 에 등록한다.**
등록하면 strictler 폴더(`~/.strictler`)로 **파일이 복사되고 해시가 함께 저장**된다.
사용자는 원본을 지워도 된다.

- **등록 시 정적 검사를 통과해야 저장된다.** 잘못된 것이 등록소에 들어가지 않는다
- **실행 시 해시를 대조**한다 — 정적 검사 루트를 피해 파일을 고치는 것을 막는다
- **해시가 그대로면 재검사하지 않는다.** 등록은 편의가 아니라 **검증 결과를 재사용하는 기제**다
- 등록 시 이름을 적지만 **참조는 자동 발급된 id 로** 한다 — `${ref.nd_e5f6a7b8}`
  (접두 `sc_`=스크립트 `lb_`=라이브러리 `nd_`=노드 `pl_`=파이프라인 `sp_`=Spec)
- **등록소는 프로젝트마다 따로 둔다** — `STRICTLER_HOME=$PROJECT_ROOT/.strictler`.
  cwd 를 거슬러 올라가는 자동 탐색은 하지 않는다 (경로 규칙과 어긋난다)
- **id 는 재현되지 않는다.** 같은 파일도 다른 등록소에선 다른 id 다 →
  **`${ref.}` 는 로컬 최적화, 절대경로+`${env.X}` 는 이식 가능.** 커밋할 것은 경로로 쓴다
- **수정은 id 를 유지한다.** 참조가 안 깨진다. 대신 **상위를 전이적으로 재검증**한다 —
  수정은 참조가 멀쩡한 채로 **검증만 조용히 무효화**하기 때문
- **삭제도 수정도 막지 않는다.** 대신 목록 조회에서 깨짐을 표시한다 —
  **참조 깨짐**(삭제) / **검증 깨짐**(수정) 두 종류

**종류 다섯에 CRUD 가 완전하다.** `<종류>` = `script` / `library` / `node` / `pipeline` / `spec`

| 명령 | 하는 일 |
|---|---|
| `strictler <종류> add <파일>` | 정적 검사 후 등록. id 발급 |
| `strictler <종류> list` | 목록. 깨진 구성 표시 |
| `strictler <종류> show <id>` | 상세 — 내용·해시·참조 관계 |
| `strictler <종류> update <id> <파일>` | 내용 교체. **id 유지.** 상위 재검증 |
| `strictler <종류> remove <id>` | 삭제 |
| `strictler node test <id>` | 노드 단위테스트 (실제 실행) |
| `strictler check <spec-id>` | 검사 실행 |

**CLI 가 유일한 표면이다.** AI 가 쓰고 등록하면 그 자리에서 걸리는 것이 저작 루프의 핵심인데,
그건 `add` 가 규칙 id·자연어 가이드·종료 코드 `2` 를 돌려주는 것으로 이미 성립한다.

**기획 데이터도 모든 노드 스크립트도 작성 주체는 AI 다.**
이 도구는 "AI 에게 스키마와 스크립트를 작성해서 넣어라"라고 시키는 도구이고,
사람이 한땀한땀 넣는 것이 **불가능할 정도로 사용법이 복잡할 것**이다. 그게 전제다.
따라서 스키마는 사람의 작성 편의보다 **AI 의 생성 용이성과 기계 검증 가능성**을 우선한다.

**사람은 스키마·스크립트 전문을 읽지 않는다. AI 가 요약한 내용을 읽고 검토·승인한다.**
→ 그래서 규칙마다 **사람이 알아볼 의도(intent) 필드**가 필요하다. 구조만 있고 의도가 없으면
   AI 요약이 구조를 되풀이할 뿐이고, 위반 리포트도 "어떤 규칙인지" 전달하지 못한다.
스키마 제약은 이제 하나다 — **AI 가 쓸 수 있을 것.**
(기획 추출 폐기로 "기계가 관측에서 생성할 수 있을 것"이라는 제약이 사라졌다)
명시되지 않은 것은 검사하지 않는다 — 기획에 적힌 것만 검사한다.

## 구현 언어 — Python + uv

**외부 도구는 철저히 외부로 분리한다.** Playwright 등은 사용자가 직접 설치하고,
경로를 Vantage config 로 받는다. strictler 는 그걸 import 하지 않는다.

**지원 스크립트 언어는 단 하나.** 크로스 랭귀지는 최소한으로 간다.
→ 엔진 언어 ≠ 스크립트 언어면 런타임을 영구 임베딩해야 하므로, **엔진 = 스크립트 언어 = Python**.

채택 근거:
- **pydantic** — strictler 의 중심 기능이 "스크립트 input/output 계약 검증"인데 이건 **런타임 검증**이다.
  TS 타입은 런타임에 소거되어 결국 zod 이중 구조가 된다. pydantic 은 타입 선언 = 검증기 = JSON Schema 생성기.
  **검증 에러가 구조화되어 AI 가 읽고 자기 수정하기 좋다.**
- **PEP 723** — 노드 스크립트가 자기 의존성을 **헤더에 선언**한다. 엔진은 이걸 읽어
  **등록 시점에 import 가능한지 확인**하고 안 되면 설치 명령을 안내한다.
  ⚠️ **격리 환경을 만들어 주지는 않는다** — 아래 참조.
- **uvx** — 런타임 버전까지 uv 가 관리한다.

**스크립트 의존성은 격리하지 않는다.** 노드 스크립트는 strictler 와 **같은 프로세스**에
로드되므로 `import` 가 strictler 환경에서 풀린다 → 외부 패키지는 그쪽에 함께 깐다
(`uv tool install strictler --with selectolax`).
**이게 lint 의 표준 모델이다** — ESLint 플러그인도 ESLint 와 같은 `node_modules` 를 쓴다.
스크립트마다 `uv run --script` 를 띄우는 안은 **일관성 때문에** 채택하지 않았다.
뒤집을 조건은 *"같은 패키지의 호환 불가 버전을 요구하는 스크립트가 등록소에 둘 이상"* 하나뿐이고,
선언된 의존성을 기록하므로 **도구가 그 조건을 스스로 검출한다.** (`docs/schema.md` 6절)
- **HTML 파싱이 오히려 빠르다** — 아래.

### HTML 파싱 실측 (4.28 MB / 231k 태그 / 버튼 24,000개)

| | |
|---|---|
| **selectolax (lexbor)** 🐍 | **78 ms** ← 기본 권장 |
| lxml 🐍 | 85 ms |
| node-html-parser ⬢ | 148 ms |
| cheerio ⬢ | 185 ms |
| BeautifulSoup(lxml) 🐍 | 1045 ms ⚠️ |

Python 파서는 **C 바인딩**(lexbor / libxml2), Node 파서는 **순수 JS** 라 CPU 바운드 파싱에서 Python 이 이긴다.
selectolax 는 트리를 C 메모리에 두고 접근 시에만 Python 객체를 만들어 대규모 DOM 에서 GC 압력이 없다.

⚠️ **BeautifulSoup 금지.** lxml 백엔드로도 13배 느리다. "Python 은 HTML 이 느리다"는 인상의 원인이 이것이다.
(벤치마크: `.claude-workspace/bench/`)

## 현재 상태

**동작한다.** 다섯 노드 타입·두 파이프라인 종류·등록소 CRUD 가 CLI 로 끝까지 돈다.

```
src/strictler/
  model/     네 층(Spec/Pipeline/Node/NodeTest)의 pydantic 모델
  typesys/   타입 어휘 + dataclass 집합 정규화 (구조 동일성·부분집합 병합)
  refs.py    ${env.X}/${config.X}/${state.X}/${ref.<id>} 전개, 절대경로 강제
  rules.py   검사 규칙 61개 + 슬롯 검증          report.py  리포트
  store/     등록소 CRUD·해시 대조·참조 그래프
  checks/    script(AST) · node · pipeline · reachability  ← 등록 시점 검사
  engine/    drive(구동 루프 정본) · state · exec · runtime(값 검증) · compare(비교)
  testing/   노드 단위테스트 하네스
  cli.py     strictler <종류> add|list|show|update|remove / node test / check
```

**종료 코드**: `0` 통과만 / `1` **위반·not run**(정상 결과) / `2` **오류**(도구가 못 돔).

| 문서 | 무엇 |
|---|---|
| **`docs/AUTHORING.md`** | **저작 가이드(독자는 AI).** 요구 → 네 층 파일, 단위테스트, 결과 읽는 법, **실패 카탈로그** |
| **`docs/schema.md`** | **확정 사항 정본.** 네 층 구조·등록소·타입 시스템·노드 계약·리포트 전부. **작업 전 먼저 읽을 것** |
| **`docs/rules.md`** | 검사 규칙 **61개** + ID 체계 + **증가 이력**(왜 늘었는지). 늘어나는 것이 전제 |
| **`docs/MODULES.md`** | 모듈 경계·공개 시그니처. **최상단 계약 개정(R1~R6)이 본문보다 우선한다** |
| `.claude-workspace/concept.md` | 설계에 이르는 논의 과정 (참고용) |

`schema.md` 16절에 **폐기된 안과 그 이유**가 정리돼 있다 — 거기 있는 것을 다시 제안하지 말 것.

**동작 예시는 `examples/home-check/`** — 다섯 노드 타입과 두 파이프라인 종류를 전부 태운다.
등록 없이 파일 경로만으로 돌고, `invalid/` 에 **일부러 틀린 것**들이 함께 있다.
`tests/test_examples.py` 가 이걸 CLI 로 태워 회귀를 막는다 — 예제가 안 돌면 **본체의 결함**이다.

## 작업 규칙

### 커밋 규칙

- **작업 단위마다 무조건 자동 커밋한다.** 짧은 작업이라도 하나 끝나면 바로 커밋한다.
  묶어서 나중에 커밋하지 않는다.
- **통합은 무조건 fast-forward merge 또는 rebase.** 3-way merge 커밋(`--no-ff`)을 만들지 않는다.
- **커밋 히스토리는 한 줄로 유지한다.** 갈라졌다 합쳐지는 그래프를 만들지 않는 것이 원칙이다.

### 기타

- 임시 파일, 스크린샷, git 에 올리지 않을 문서는 전부 `.claude-workspace/` 아래에 만든다.
- 확정되지 않은 설계 결정을 임의로 확정하고 구현하지 않는다. `concept.md` 의 미결 질문에 해당하면 먼저 확인한다.
